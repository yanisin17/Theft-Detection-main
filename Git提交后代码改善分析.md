# 最近一次 Git 提交后的代码改善分析

## 分析基准

- 最近一次提交：`8e3dd82e5ba21ec194db347df06c26ce13272053`
- 提交时间：`2026-09-16 16:30:51 +0800`
- 提交说明：`补充文件`
- 分析日期：`2026-09-18`
- 比较方式：以 `HEAD` 为基准，同时检查已修改、已暂存删除和未跟踪文件

> 当前改动尚未形成新的 Git 提交。本文分析的是最近一次提交之后的全部工作区变化，不等同于已发布版本的稳定结论。

## 一、总体结论

本轮改动的核心目标不是单纯增加功能，而是修复实时偷盗检测链路中会导致漏报、误报和显示错位的几个关键问题，并补强配置安全、摄像头断线恢复和回归测试。

改善主要集中在以下方向：

1. **采集链路稳定性**：RTSP 强制 TCP、超时、断线重连、最新帧缓存和运行统计。
2. **检测准确性**：修复多人跟踪状态串流、MediaPipe 关键点索引错误、裁剪图坐标未转换、实时状态机采样过慢等问题。
3. **报警召回率**：调整藏匿行为分数和报警阈值，并暂时关闭当前无法可靠成立的二次佐证路径。
4. **前后端一致性**：持续传递实际画面尺寸、结构化报警信息和微秒级时间戳。
5. **配置与部署安全**：摄像头凭据改为环境变量引用，默认仅监听本机，避免关键配置继续进入 Git。
6. **验证能力**：新增多组单元测试，对报警文件名、可视化叠加和藏匿评分进行回归验证。

从影响看，本轮修复中优先级最高的是 **MediaPipe 33 点索引修正、报警字段兼容、多摄像头独立 tracker、裁剪检测框坐标平移**。这些属于原代码中会直接破坏检测逻辑的功能性缺陷。

## 二、改动范围

### 已跟踪文件

本轮相对 `HEAD` 的已跟踪改动合计约 `+416 / -942` 行。删除行数较多，主要来自删除旧配置文件和旧分析报告，并非业务代码大规模删减。

| 文件或目录 | 主要变化 |
| --- | --- |
| `backend.py` | 采集线程、重连统计、tracker 隔离、检测坐标、可视化顺序、报警兼容和安全监听 |
| `detection_engine/models/behavior/video_behavior.py` | 修正 7 个行为检测函数中的 MediaPipe 关键点索引 |
| `detection_engine/realtime_behavior.py` | 调整藏匿行为报警分算法和字段名 |
| `detection_engine/detector_config.py` | 调整报警阈值、姿态模型复杂度和佐证策略 |
| `dashboard/src/app/cameras/page.tsx` | ROI 画布使用后端实际视频尺寸 |
| `dashboard/src/app/history/page.tsx` | 兼容微秒级历史时间戳 |
| `dashboard/src/components/CameraGrid.tsx` | 使用结构化报警字段并显示中文行为名称 |
| `.env.example`、`.gitignore` | 增加摄像头环境变量模板并忽略本地敏感配置 |
| `cameras.json` | 从 Git 跟踪中移除，避免摄像头账号密码入库 |
| `项目问题分析报告.md` | 从 Git 跟踪中删除，由新的分析报告替代 |

### 新增但未跟踪文件

| 文件 | 作用 |
| --- | --- |
| `cameras.json.example` | 使用环境变量占位符的摄像头配置模板 |
| `tests/test_backend_alert_filename.py` | 验证报警截图文件名使用微秒精度 |
| `tests/test_backend_visualization.py` | 验证 `plot()` 之后绘制的自定义标注可以保留 |
| `tests/test_realtime_behavior.py` | 验证藏匿事件的 `alert_score` 范围 |
| `codex-项目分析.md`、`修复报告.md` | 项目问题分析和修复说明 |
| `tools/remove_methods.py`、`tools/remove_ml_chain.py` | 一次性维护脚本，用于删除旧代码链 |
| `test_result.txt` | 历史测试输出，约 11 MB，不是当前测试结论 |
| `tmp_eval_frames/_ws_frame.*` | 临时画面样本，不应作为源码提交 |

## 三、主要改善

### 1. 摄像头采集链路更稳定

`backend.py` 中的 `ThreadedCamera` 得到明显增强：

- RTSP 在创建第一个 `VideoCapture` 之前设置 `OPENCV_FFMPEG_CAPTURE_OPTIONS`，强制使用 TCP，并兼容设置 5 秒 socket 超时。
- 每路摄像头独立采集，只保留最新帧，避免推理线程反复处理已经过期的画面。
- 设置较小的 `CAP_PROP_BUFFERSIZE`，减少解码队列带来的实时画面延迟。
- 连续读取失败 15 次后自动释放旧连接并重连，重连间隔从 2 秒指数退避到最多 30 秒。
- 记录实际宽高、FPS、读取帧数、丢帧数、读取延迟、重连次数和当前状态。
- 暴露帧时间戳，WebSocket 消息和画面右上角时间均使用采集时间，便于判断是否显示旧帧。
- 删除摄像头后会清理对应的独立 pose tracker，避免模型实例持续占用资源。

这些改动会直接降低 RTSP 丢包、断流后画面永久卡死以及推理处理旧帧的概率。

### 2. 修复多摄像头跟踪状态互相污染

原实现让多个摄像头交替调用同一个 `YOLO.track(..., persist=True)` 实例。由于 tracker 内部保存卡尔曼状态和轨迹历史，交叉输入不同视频流会造成轨迹预测失准和 `track_id` 跳变。

现在改为：

- 每个 `cam_id` 使用独立的 YOLO pose tracker。
- 行为状态继续按 `cam_id:track_id` 隔离。
- 同一人的“伸手→回缩”事件不会再因为 tracker ID 跳变而被拆成两个状态。

这是实时行为状态机能够稳定形成完整动作序列的重要前置修复。

### 3. 修复检测图片和检测框坐标不一致

原来人体被裁剪成 `sub_frame` 后送入行为引擎，但商品检测框仍然是整帧坐标，人体手腕位置与商品框无法正确对应。

现在会：

- 将商品框整体减去裁剪区域左上角偏移，转换到裁剪图坐标系。
- 将检测结果从不稳定的 ultralytics 对象属性访问改为明确的列表字典格式。
- 保留类别名和置信度，便于后续关联逻辑使用。
- 对象检测不是当前帧执行时，丢弃超过 1 秒的旧检测框。
- 人脸、人体裁剪、行为引擎统一从未标注的 `clean` 帧取值，避免把绘制内容再次送入模型。

这会改善“人物与商品空间关联”的正确性，也是本轮最关键的算法链路修复之一。

### 4. 修正 MediaPipe 33 点关键点索引

`video_behavior.py` 中多个函数原先按 COCO 17 点格式读取 MediaPipe 33 点数据，导致肩、肘、腕、髋、膝、耳朵等部位全部错位。例如旧的 `landmarks[5]` 实际读取的是眼睛附近，而不是肩膀。

本轮已将相关检测函数改为 MediaPipe 索引：

| 部位 | 修正后索引 |
| --- | --- |
| 左右肩 | `11 / 12` |
| 左右肘 | `13 / 14` |
| 左右腕 | `15 / 16` |
| 左右髋 | `23 / 24` |
| 左右膝 | `25 / 26` |
| 左右耳 | `7 / 8` |

同时同步提高关键点最小数量检查，避免数据不足时继续执行错误判断。受影响的规则包括异常手臂姿态、手在背后、异常伸手、蹲姿、身体遮挡、头部运动和物品抓取藏匿等。

### 5. 提高实时动作采样的连续性

实时状态机原先把 `BEHAVIOR_CHECK_INTERVAL = 6` 同时用于聚合行为引擎和“伸手→回缩”检测。在多摄像头串行推理时，同一个人可能每隔 2 至 4 秒才被分析一次，而完整动作通常只持续 1 至 2 秒，导致关键帧大量遗漏。

现在拆分为：

- `BEHAVIOR_CHECK_INTERVAL = 6`：低频聚合行为规则。
- `REALTIME_CHECK_INTERVAL = 1`：实时状态机每帧分析。

这提升了短时动作的召回基础，但也会增加 MediaPipe 推理开销，需要在真实多路摄像头环境中验证 CPU/GPU 余量。

### 6. 调整藏匿行为的评分与报警策略

`realtime_behavior.py` 和 `detector_config.py` 做了以下校准：

- 藏匿事件字段由 `confidence` 改为更准确的 `alert_score`。
- 分数公式由 `0.72 + excess_ratio * 0.18` 调整为 `0.50 + excess_ratio * 0.30`。
- 分数范围限制为 `0.45` 至 `0.85`。
- 偷盗报警阈值由 `0.7` 调整为 `0.6`。
- MediaPipe Pose complexity 由 `0` 提高到 `1`，换取更稳定的关键点。
- `REQUIRE_CORROBORATION` 暂时从 `True` 改为 `False`，让单次完整藏匿动作可以直接报警。

这项调整的目标是解决典型真实事件得分只有 `0.55` 至 `0.65`、永远无法越过旧 `0.7` 阈值的问题。

但需要明确：这是一项偏召回率的临时校准，不代表模型准确率已经通过真实数据证明。关闭二次佐证通常会提高召回，也会提高误报风险，后续应在有标签的真实数据上重新决定是否开启。

### 7. 修复报警字段不一致导致的“检测到但不报警”

实时状态机改成返回 `alert_score` 后，原有后端仍读取 `confidence`，会让藏匿事件被当作 0 分处理。

现在后端统一兼容读取：

```python
alert_score -> confidence -> 0.0
```

该兼容逻辑已经覆盖实时视频循环、图片分析和视频分析三条路径。同类字段问题也补充了单元测试。

### 8. 修复可视化内容被 YOLO 绘图覆盖

旧流程先绘制热力图和自定义标注，之后再次执行 `results_pose[0].plot()` 并覆盖整个 `frame`，导致行为文字、告警框和热力图可能全部消失。

新顺序调整为：

1. 在原始帧上执行模型推理。
2. 先生成 YOLO 骨架和检测框。
3. 再叠加热力图、行为文字、ROI 和报警框。
4. 最后添加画面采集时间戳。

这样能够保证自定义标注保留在最终展示帧中。

### 9. 前端 ROI 与报警显示更加准确

摄像头页面不再把 ROI 画布固定为 `1280x720`，而是：

- 后端在 WebSocket 消息中返回实际视频 `width`、`height`。
- 前端按实际尺寸建立画布和坐标系统。
- 页面不再强制 `aspect-video`，避免宽高比不一致时画面拉伸或坐标偏移。

历史页面同时支持以下两种报警时间格式：

```text
YYYYMMDD_HHMMSS
YYYYMMDD_HHMMSS_ffffff
```

报警浮层改为优先读取后端结构化字段 `behavior_type`、`confidence`，并增加中文行为名称映射，消息文本仅作为回退方案。

### 10. 配置和部署安全得到增强

- 摄像头 RTSP 地址可使用 `${CAM_USER}`、`${CAM_PASS}` 环境变量占位符。
- 真实 `cameras.json` 已从 Git 索引移除，并加入 `.gitignore`。
- `.env` 已加入 `.gitignore`。
- `.env.example` 新增摄像头账号密码模板。
- 新增不含真实凭据的 `cameras.json.example`。
- uvicorn 默认从 `0.0.0.0` 改为 `127.0.0.1`，局域网访问需要通过 `HOST=0.0.0.0` 显式开启。

### 11. 证据文件不再因同秒多报警互相覆盖

报警截图文件名时间部分从：

```text
%Y%m%d_%H%M%S
```

改为：

```text
%Y%m%d_%H%M%S_%f
```

同一秒内产生多个报警时，不再覆盖前一个证据截图。历史页面也已兼容新的微秒格式。

### 12. 测试与问题定位能力增强

新增测试覆盖：

- 微秒级报警文件名格式及同秒不同文件。
- YOLO `plot()` 之后自定义绘制内容仍然存在。
- 藏匿事件返回 `alert_score` 且位于预期区间。
- 新报警阈值和配置默认值。

`tests/test_alert_policy.py` 也同步更新了阈值、二次佐证和 MediaPipe complexity 的断言。

## 四、验证结果

本次分析重新执行了以下检查：

| 检查项 | 结果 |
| --- | --- |
| `git diff HEAD --check` | 通过，仅提示部分文件 LF/CRLF 换行格式 |
| Python `py_compile` | 通过 |
| `python -m unittest discover -s tests -v` | `14/14` 通过 |
| 前端 `tsc --noEmit` | 通过 |
| 多路真实 RTSP 长时间稳定性 | 未执行 |
| ROI 点击与后端坐标一致性端到端测试 | 未执行 |
| 证据视频生成与播放链路测试 | 未执行 |
| 真实标注集 Precision / Recall / F1 | 尚未建立 |
| 多摄像头 CPU/GPU、延迟和内存压测 | 未执行 |

`test_result.txt` 是旧的历史运行输出，其中包含旧版本测试失败记录和 RTSP 未授权日志，不能代表本轮当前代码状态。本次重新执行的测试结果为全部通过。

## 五、改善效果判断

### 高可信改善

以下问题属于明确的逻辑错误或链路错误，本轮修复具有较高确定性：

- MediaPipe 33 点使用了错误的 COCO 索引。
- 多摄像头共用有状态 tracker 导致轨迹 ID 跳变。
- 裁剪图检测框没有做坐标平移。
- 报警事件使用 `alert_score`，后端却读取 `confidence`。
- `plot()` 在自定义标注之后执行并覆盖画面。
- 固定 ROI 画布尺寸与实际视频尺寸不一致。
- 微秒时间戳未被历史页面解析。

### 需要真实数据验证的改善

以下调整更偏向工程调参，不能仅凭单元测试判断最终效果：

- 报警阈值从 `0.7` 调整到 `0.6`。
- 藏匿行为分数公式重新缩放。
- 暂时关闭二次佐证。
- MediaPipe complexity 从 `0` 调整到 `1`。
- 实时状态机由每 6 帧分析改为每帧分析。

这些改动的预期方向是提高真实事件召回率，但也可能增加误报和计算开销。

## 六、仍需关注的问题

1. **摄像头密码仍可能存在于 Git 历史中。** 当前只是从最新索引移除，旧提交仍可能保留明文。最稳妥的处理是立即更换摄像头密码；如果需要清理历史，再使用专门的 Git 历史重写工具。
2. **对象模型仍可能是通用 COCO 模型。** 这类模型无法可靠区分货架商品、正常拿取和真实藏匿，系统目前仍主要依赖人体规则和空间关联推断。
3. **关闭二次佐证后误报风险上升。** 应在商品检测模型或可靠二次证据就绪后重新评估。
4. **缺少真实场景评测。** 目前没有 Precision、Recall、F1、误报率、漏报率和报警延迟等业务指标。
5. **多摄像头性能尚未验证。** 每路独立 YOLO tracker 和每帧 MediaPipe 分析会更准确，但也会增加 CPU、GPU 和内存压力。
6. **测试仍以单元测试为主。** 还不能证明完整 RTSP 采集、跟踪、行为识别、证据保存和 WebSocket 展示链路端到端可靠。
7. **部分未跟踪文件不应直接提交。** `test_result.txt` 约 11 MB，`tmp_eval_frames/` 属于临时样本；应删除或加入忽略规则。
8. **一次性维护脚本需要审查。** `tools/remove_methods.py` 和 `tools/remove_ml_chain.py` 会直接重写 `video_behavior.py`，不适合作为生产运行工具，建议确认用途后再决定是否保留。

## 七、建议的后续优先级

1. 更换真实摄像头密码，并确认远端仓库的历史泄露风险。
2. 使用实际摄像头连续运行至少数小时，记录每路 FPS、读取延迟、丢帧、重连次数和 CPU/GPU 使用率。
3. 录制并标注正常拿取、放回、真实藏匿、整理衣物、弯腰和遮挡商品等样本。
4. 建立 Precision、Recall、F1、每小时误报数和报警延迟指标，再决定最终阈值与佐证策略。
5. 将本轮改动按“安全配置”“采集与检测修复”“前端协议”“测试与文档”拆分提交，避免一次性提交难以审查。
6. 清理历史测试输出和临时帧，只保留可重复执行的测试代码与必要文档。

## 八、结论

本轮改动是一次面向实际问题的集中修复。它解决了多个会直接导致检测失效的逻辑缺陷，并显著改善了摄像头采集稳定性、坐标一致性、报警字段兼容、安全配置和前端显示。

其中算法链路修复可信度较高，但阈值、佐证策略和采样频率仍属于基于场景经验的调参。下一步的关键不是继续堆叠规则，而是通过真实摄像头和带标签数据验证召回率、误报率与性能，再据此完成最终校准。
