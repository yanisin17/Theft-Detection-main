# TheftGuard AI 项目问题分析报告

> 分析对象：`E:\Latest-Theft-Detection-main\Theft-Detection-main`
> 分析方式：全量源码阅读 + 依赖/运行环境核对 + 动态复现验证（含 1 处死锁实测）
> 分析时间：2026-09-16

---

## 0. 结论速览（先看这里）

| 级别 | 问题 | 位置 | 后果 |
| --- | --- | --- | --- |
| **P0** | `save_camera_roi` 在持有非重入锁时调用 `save_cameras`，自身再次抢同一把锁 | `backend.py:688-698` + `backend.py:544-559` | **前端点“保存 ROI”会永久挂死整个后端**（已实测：进程不再响应，事件循环与视频循环一起卡死） |
| **P0** | 唯一的“ML 盗窃模型”实际不可用：模型是 30 维 6 分类，代码按 12/16 维二分类调用 | `models/theft_xgb_model.pkl` / `video_behavior.py:560-639` / `detection.py:1038-1065` | ML 通路 100% 抛异常后静默回退规则引擎，`ML Detected Theft` 永远不会触发 |
| **P0** | 前端“保存 ROI”正是触发上述死锁的操作，且是配置摄像头的必经步骤 | `dashboard/src/app/cameras/page.tsx:233-254` | 用户按 README 走完摄像头配置流程，系统就假死 |
| P1 | 报警策略与历史数据自相矛盾：库里 31 条报警全是 `suspicious_crouching` / `looking_around` 等中低风险行为且 `sequence=N`，按当前策略一条都不该报 | `theft_detection.db` / `alert_policy.py:9,21-34` / `backend.py:1274-1313` | 现有标注集不能用作评估基线；策略反复漂移且无回归保护 |
| P1 | 实时通路的“置信度”是公式合成的，恒 ≥ 0.72 | `realtime_behavior.py:265-281` | `THEFT_ALERT_THRESHOLD=0.7` 对实时通路形同虚设，状态机一触发必报警 |
| P1 | 聚合通路把“人物裁剪图”和“全帧坐标的检测结果”混着用 | `backend.py:1174-1184` → `video_behavior.py:1092-1147,1170-1187` | 该通路的行为/物品交互判断坐标系错位，结果不可信 |
| P1 | 每帧 `frame = results_pose[0].plot()` 覆盖了此前所有标注与热力图 | `backend.py:1360-1364` | 偷盗概率、行为列表、ITEM 框、PENDING 提示、热力图全部看不到，证据视频里也没有 |
| P1 | `async` 接口里跑数十秒的同步 AI 推理，且每次请求重新加载模型 | `backend.py:1655-1805` | 一次离线分析会把 WebSocket 实时画面和所有 API 一起卡住 |
| P1 | 删除单条告警会删掉 `evidence_videos` 下**所有** mp4 | `backend.py:437-441` | 删 1 条丢全部证据视频（当前约 103MB 数据） |
| P1 | 告警截图文件名只精确到秒 | `backend.py:1440-1442` | 同秒同机位第二次报警覆盖第一次截图（库里已有同秒重复记录） |
| P1 | 依赖清单与实际 venv 严重脱节（numpy 1.26.4 vs 2.2.6 等 12 项） | `requirements.txt` vs `.venv` | 换机器按 requirements 装未必能跑；按现状装又与声明不符 |
| P1 | 三套并行实现，其中两套导入路径已失效 | `detection_engine/app.py:30-32`、`detection_engine/ui/main_window.py:19-20` | `src/` 目录只剩一个 config，`import src.models...` 必然 ImportError，两个入口是纯死代码 |
| P1 | 摄像头明文 RTSP 账号密码已提交进 git | `cameras.json:5,11,17`、`diag_engine.py:49` | 凭据泄露；且 `.gitignore` 未忽略 `.env`，而 `save_settings` 会把密码写进 `.env` |
| P1 | 38 个关键文件被企业 DLP 加密（`%TSD-Header-###%` 文件头） | 见 §8 清单 | 脱离本机白名单进程（CI、Docker、WSL、换机器）读到的是密文，含 `requirements.txt`、全部前端源码、全部模型权重 |

---

## 1. 项目现状梳理

### 1.1 目录与代码规模（真实行数）

| 文件 | 行数 | 说明 |
| --- | --- | --- |
| `backend.py` | 1810 | 在用的 FastAPI 主服务（视频循环、告警、人脸、证据、离线分析） |
| `detection_engine/ui/main_window.py` | 4273 | Tkinter 桌面 GUI（**已失效**） |
| `detection_engine/models/behavior/video_behavior.py` | 4003 | 21 种行为引擎（视频） |
| `detection_engine/models/behavior/image_behavior.py` | 2933 | 21 种行为引擎（图像） |
| `detection_engine/models/detection.py` | 1836 | `TheftDetector` + `ObjectDetector` |
| `detection_engine/models/object_detection.py` | 715 | 又一个 `ObjectDetector`（与上一个重复） |
| `detection_engine/app/ui.py` | 681 | 又一套 Tkinter GUI |
| `detection_engine/app.py` | ~600 | Flask Web 应用（**已失效**） |
| `detection_engine/realtime_behavior.py` | 302 | 实时“伸手→回缩藏匿”状态机（**当前唯一有效报警源**） |
| `detection_engine/offline_pipeline.py` | 274 | 离线回放/调参（backend 逻辑的手抄副本） |
| `dashboard/src/**` | ~1700 | Next.js 16 + React 19 前端 |

合计约 1.9 万行 Python + 1.7 千行 TS/TSX，没有分层、没有服务层、几乎没有测试。

### 1.2 实际数据流（读码得出，与 README 描述不一致）

```
RTSP/USB 摄像头
  └─ ThreadedCamera（每路一个读线程）              backend.py:466-518
     └─ video_loop（单线程 while True）            backend.py:889-1432
        ├─ YOLOv8n-pose.track（每帧，多路共用一个模型）  backend.py:972
        ├─ [实时通路] RealtimeBehaviorDetector(每6帧)   backend.py:1101-1171
        │     └─ alert_policy.should_trigger_theft_alert(sequence_confirmed=True)
        ├─ [聚合通路] VideoBehaviorDetector(每6帧)      backend.py:1174-1316
        │     └─ 加权 + 连续性 + 序列 → 偷盗概率
        ├─ ROI 入侵 / 徘徊 / 弯腰 / 黑名单人脸
        ├─ 证据视频（前5s + 后5s）                     backend.py:1378-1405
        └─ 打包 base64 帧 → WebSocket（约 25fps）       backend.py:1415-1424
```

README 宣称的“21 种行为 + XGBoost 分类”与实情不符：**XGBoost 从未真正参与决策**（见 §3.2）。

---

## 2. P0：会直接把系统搞死的问题

### 2.1 【已实测复现】保存 ROI 触发死锁，整个后端挂死

```python
# backend.py:544  —— save_cameras 自己会抢锁
def save_cameras(self):
    ...
    with self.lock:                      # ← threading.Lock()，非重入
        for cam_id, cam_data in self.cameras.items():
            ...

# backend.py:688  —— 路由已经拿着同一把锁，再调 save_cameras
@app.post("/cameras/{camera_id}/roi")
async def save_camera_roi(camera_id: str, data: dict):
    if "points" in data:
        with camera_manager.lock:        # ← 持锁
            if camera_id in camera_manager.cameras:
                camera_manager.cameras[camera_id]["roi_points"] = points
                camera_manager.save_cameras()   # ← 再次抢同一把非重入锁 → 永久阻塞
                return {...}
```

**为什么是致命的**

- `threading.Lock` 不可重入，第二次 `acquire()` 直接死等。
- 死锁发生在**事件循环线程**内（`async def` 路由体全是同步代码），所以不是“一个请求挂住”，而是**整个 uvicorn 事件循环冻结**：`/ws` 停止推送、所有 API 不再响应、前端全站假死。
- `video_loop` 每轮开头就有 `with camera_manager.lock:`（`backend.py:947`），下一轮立即卡在同一把锁上 → **采集与检测线程也一起停**。
- 没有任何超时或看门狗，只能杀进程。

**实测证据**：构造 `CameraManager`（cwd 切到临时目录，不触碰仓库文件）后调用 `save_camera_roi`：

```
结果：进程永久挂起 —— 连 asyncio.wait_for(..., timeout=5) 都无法触发超时，
      因为阻塞点就在事件循环线程内部；最终需手工 kill python 进程才退出。
```

**修复方向**

1. 把落盘逻辑拆成不抢锁的 `_save_cameras_unlocked()`，由调用方决定是否持锁；或把 `self.lock` 换成 `threading.RLock()`。
2. 更干净：`save_camera_roi` 先改内存，退出 `with` 之后再落盘。
3. 注意 `add_camera()`（`backend.py:574-594`）与 `remove_camera()`（`596-606`）**恰好在锁外**调用 `save_cameras()`，所以没踩坑——说明这是遗漏而非设计，同文件已有正确写法可参照，缺少统一约定。

---

## 3. AI 决策链路基本不成立

### 3.1 报警策略只认一种行为，历史数据与策略完全对不上

```python
# detection_engine/alert_policy.py:9
_ALERTABLE_BEHAVIORS = {'rapid_item_concealment'}

# alert_policy.py:21-34
def should_trigger_theft_alert(behavior_type, confidence, sequence_confirmed=False, threshold=None):
    if not is_alertable_behavior(behavior_type):  return False   # 行为白名单
    if not sequence_confirmed:                     return False   # 必须序列确认
    return float(confidence) >= threshold                          # 阈值
```

**数据库里 31 条报警的真实分布**（`theft_detection.db` → `alerts`）：

| behavior_type | 条数 | 说明 |
| --- | --- | --- |
| `suspicious_crouching` | 17 | 属 MEDIUM 档，不在白名单 |
| `looking_around` | 11 | 属 LOW 档（正常逛店看四周） |
| `unusual_arm_position` | 3 | 属 MEDIUM 档 |

这 31 条**全部**是 `sequence=N`，置信度最低 **0.51**、最高 0.85，消息格式全部来自聚合通路：
`THEFT: {行为} | 概率=… | rule=… continuity=… sequence=N`。
按现在的 `alert_policy`，这 31 条**一条都不会触发**。

结论：

1. 历史报警由旧策略产出（当时 `looking_around=0.53` 也会报警），说明**误报曾大范围存在**——“看四周”被判成盗窃。
2. 当前数据库 / 截图 / 证据视频**不能**作为评估基线（`labels/alerts_to_label.csv` 的 `is_true_positive` 列目前全空，且标注的也是旧策略行为）。
3. 策略缺乏回归保护：`tests/test_alert_policy.py` 只覆盖 `alert_policy` 本身，没有任何测试约束 `backend.py` 的两条通路。

### 3.2 XGBoost 模型：加载了，但从未生效

用项目自带 `.venv` 直接加载模型，实测属性：

```
models/theft_xgb_model.pkl  →  dict{'model': XGBClassifier, 'label_encoder': LabelEncoder}
XGBClassifier.n_features_in_ = 30
XGBClassifier.classes_       = [0 1 2 3 4 5]   ← 6 分类（6 种行为），不是“盗窃/正常”二分类
```

而调用方是这样用的：

| 位置 | 问题 |
| --- | --- |
| `video_behavior.py:621-622` | `predict([features])` / `predict_proba([features])[0][1]` 按**二分类**取第 1 类概率；对 6 分类模型，“class 1”只是第 2 种行为 |
| `video_behavior.py:747-947` | `_extract_standard_features` 实际只 append 了 **12 个特征**，与 30 维模型不匹配 → `predict` 直接抛 shape 异常 |
| `video_behavior.py:635-639` | 异常被 `except` 吞掉后 `return 0, 0.0` → 静默失效 |
| `video_behavior.py:1130-1137` | 需 `prediction == 1 and probability > 0.6` 才产出 `"ML Detected Theft"` → **永不成立** |
| `detection.py:126-129` | `joblib.load()` 拿到 dict，却把 `theft_classifier_type` 置为 `"ml-model"` |
| `detection.py:1038` | 门禁通过（看起来“有模型”） |
| `detection.py:1059` | `self.theft_classifier.predict_proba(...)` —— 对 **dict** 调方法 → AttributeError |
| `detection.py:1049-1056` | 即便解包了模型，也是把特征补零到 **16 维** 去喂 30 维模型 |
| `detection.py:1063-1065` | 异常被捕获 → 仅 `logger.warning` 后回退规则引擎，用户完全无感 |
| `image_detection.py:62-90` | 同样的 dict 问题（`self.standard_model` 存的就是 dict） |

**影响**：所谓“XGBoost 行为分类”是纯装饰。真正决定报警的是 `realtime_behavior.py` 的几何状态机 + 规则加权；README 中“ML Detected Theft”一类描述属于不实宣传。

**附带的模型/环境问题**（`.venv` 实测）：

- 模型由 sklearn 1.6.1 训练，当前 1.7.2 加载 → `InconsistentVersionWarning`（提示可能产生错误结果）。
- XGBoost 旧序列化格式警告（官方建议改用 `Booster.save_model`）。
- `models/theft_xgb_model.pkl` 与 `models/standard/theft_xgb_model.pkl` 是两份**完全相同**的 1.4MB 副本。

### 3.3 实时通路的“置信度”是人为合成的常数段

```python
# detection_engine/realtime_behavior.py:265-281
excess_ratio = min(1.0, excess / self.retreat_min)
base = 0.72 + excess_ratio * 0.18                      # → 0.72 ~ 0.90
pos_bonus = 0.05 if conceal_sig["on_hip_bag"] else (0.02 ...)
fierce_bonus = reach_fierce * 0.04
conf = float(max(0.72, min(0.96, base + pos_bonus + fierce_bonus)))
return {"type": "Rapid Item Concealment", "confidence": conf, ...}
```

- 只要状态机跑完“探出→回缩”，`conf` 必落在 **[0.72, 0.96]**，而报警阈值是 **0.7**。
- 即：**阈值 0.7 对该通路毫无约束**，`conf` 大小只影响告警文案，不影响是否报警。
- 更糟的是这个数字会被写进数据库 `confidence` 字段、被 `update_alert_peak` 当作“峰值置信度”、被前端当作“模型可信度”展示——**它不是概率，是人为打分**，对外有严重误导性。
- 同理，`alert_policy` 中“置信度达标”这一层对实时通路是空转；对聚合通路（另一套 0.53~0.85 的加权分）才有约束——两条通路分数量纲不同却共用同一个阈值比较。

### 3.4 聚合通路坐标系混用（结果不可信）

```python
# backend.py:1174-1184
if run_behavior and should_run_aggregate_detector(realtime_confirmed):
    pad = 60
    sx1 = max(0, box[0] - pad); ...
    sub_frame = frame[sy1:sy2, sx1:sx2].copy()
    all_behaviors = behavior_detector.detect_behaviors_in_image(sub_frame, results_pose[0])
                                                          #  ↑ 人物裁剪图      ↑ 全帧坐标的 Results
```

- `sub_frame` 是**人物局部裁剪**，而 `results_pose[0]` 的 `boxes / cls / keypoints` 全是**原始全帧坐标**。
- 进入 `video_behavior.py:_detect_behaviors_with_rules`（1170-1187）后，用全帧 bbox 在裁剪图里做人/物关系判断；`_extract_pose_landmarks(image)` 又是在裁剪图上跑 MediaPipe，得到**归一化到裁剪图**的关键点，再与全帧坐标的 `object_detections` 做交互判断（`_detect_unusual_reaching` 等）。
- 结果：该通路的 `rule_prob / continuity / sequence` 建立在错位坐标上，`sequence_detected` 也因此不可信——而它正是聚合通路能否报警的关键开关（`backend.py:1297-1300`）。
- 另外 `VideoBehaviorDetector` 内部还持有跨调用共享的 `self.prev_landmarks`（`video_behavior.py:1139-1140`）、`self._last_person_positions`、`_loitering_count` 等状态；多路摄像头/多人共用同一实例时会互相污染。

### 3.5 每帧 `plot()` 把全部可视化覆盖掉

```python
# backend.py:1360-1367
frame = get_heatmap_overlay(cam_data, frame)      # ① 画热力图
if results_pose[0].keypoints is not None:
    res_plotted = results_pose[0].plot()          # ② 生成一张全新图像 …
    frame = res_plotted                           #    … 直接替换掉 frame
if len(cam_roi) > 0:
    cv2.polylines(frame, [np.array(cam_roi)], ...)  # ③ 只有 ROI 因画在后面而幸存
```

被覆盖掉的内容（全部绘制于 1360 行之前）：

- `偷盗概率: xx%`（`backend.py:1281-1283`）
- 参与计算的行为小字（`1286-1293`）
- `PENDING CONFIRM (concealment)` 挂起提示（`1167-1170`）
- 红色报警框（`1162`、`1312`）
- `ITEM: xxx` 物品框（`1011-1013`）、`BENDING`（`1333`）、徘徊计时（`1349`）
- ①刚画好的热力图

即：**实时画面上用户只能看到骨架 + ROI 框**，README 大书特书的“偷盗概率标注”“活动热力图”在浏览器里根本看不到。

连带影响：

- 证据视频缓冲存的是被覆盖后的帧（`backend.py:1379-1380`），所以证据视频同样没有标注；
- 而 `trigger_alert(... frame ...)` 在覆盖之前调用，**报警截图反而带标注** → 截图与证据视频内容不一致，事后取证容易误判；
- `results_pose[0].plot()` 每帧重新分配整张图，纯属性能浪费。

补充：`showHeatmap` 设置**只出现在 `SettingsModel` 定义里**（`backend.py:131`，全仓出现 1 次），既没读也没用 → 前端的热力图开关是个装饰。

---

## 4. 运行时稳定性与数据安全

### 4.1 `async` 接口里跑重 CPU 任务，阻塞整个服务

| 位置 | 问题 |
| --- | --- |
| `backend.py:1655-1723` `analyze_image` | `async def` 内 `YOLO('yolov8n-pose.pt')` + `RealtimeBehaviorDetector()`，**每次请求重新加载模型** |
| `backend.py:1726-1805` `analyze_video` | 同上，且逐帧 `pose.track()` + MediaPipe；前端自己标注“可能需要 10-60 秒” |

后果：FastAPI 事件循环被同步阻塞数十秒 → 期间 `/ws` 不推帧、`/cameras`、`/history` 全部超时，前端整体卡死。按钮文案说明作者知道它慢，却仍放在主事件循环里。

修复方向：模型改为全局单例（或 `lru_cache`）；耗时任务丢到 `run_in_threadpool` / 独立进程，并配任务队列 + 进度查询接口。

### 4.2 删除一条告警 = 删掉全部证据视频

```python
# backend.py:436-441
if video_path:
    for fname in os.listdir(EVIDENCE_DIR):
        if fname.endswith(".mp4"):
            try: os.remove(os.path.join(EVIDENCE_DIR, fname))   # ← 删光所有 mp4
            except Exception: pass
```

注释写的是“同名 alert_id 前 8 位匹配”，代码却是**无条件遍历删除**。`evidence_videos/` 当前有 17 个 mp4（合计约 103MB），删任意一条告警就会全灭。

### 4.3 证据 / 截图文件互相覆盖

```python
# backend.py:1440-1442
timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")     # 秒级
filename = f"alerts/alert_{cam_id}_{timestamp}.jpg"       # 同机位同秒 → 同名
cv2.imwrite(filename, frame)
```

数据库中已存在同秒重复记录（如 `20260915_164734` 两条、`20260915_173140`/`173141` 各两条、`20260916_091724` 两条）→ 同一秒的第二次报警会覆盖第一次截图，但数据库里是两行指向同一文件，取证时会看到两张相同的图。

### 4.4 视频采集：无丢帧、无重连、无释放

```python
# backend.py:495-505
def update(self):
    while self.running:
        if self.cap.isOpened():
            ret, frame = self.cap.read()
            ...
            time.sleep(0.01)
```

- 读线程以最快速度读并只保留最新帧，但**没有按目标帧率主动丢弃**；而主循环 `backend.py:1426` 是 `time.sleep(0.04)` + 每帧对每路跑一次 YOLO pose，真实帧率远低于 25fps → 网络流缓冲堆积，**画面延迟随运行时间持续增长**（典型的“越跑越慢”）。
- RTSP 断开后多数后端 `self.cap.isOpened()` 仍返回 True，`read()` 持续失败但**没有重连逻辑**（`backend.py:966-967` 只画一张静态“无信号”图），必须重启进程。
- `release()`（`516-518`）只置标志 + `cap.release()`，**不 join 线程**，存在“线程正在 read 时对象被释放”的竞态。
- `add_camera()` 失败分支（`592-594`）中，已创建的 `ThreadedCamera` 被丢弃且**从未 `release()`** → 句柄泄漏；反复添加错误地址会持续泄漏摄像头句柄。

### 4.5 多路共用同一个 Ultralytics tracker

```python
# backend.py:972
results_pose = model_pose.track(frame, persist=True, verbose=False, classes=[0])
```

`persist=True` 复用同一个 tracker 实例；多路视频轮流喂进**同一模型实例**，tracker 的 ID 分配跨相机共享。代码用 `(cam_id, track_id)` 复合键只是把“状态串号”降级为“ID 可能串号”，无法阻止 tracker 在相机间交换 ID（两路同时有人进出时尤其明显）。

### 4.6 人脸识别被放在每帧热路径上

```python
# backend.py:1044-1055
if FACE_REC_AVAILABLE and (not p_state.face_checked or (current_time - p_state.face_check_time > 2.0)):
    face_locs = face_recognition.face_locations(rgb_face)       # dlib，CPU 重
    encodings = face_recognition.face_encodings(rgb_face, face_locs)
    matches = face_recognition.compare_faces(known_face_encodings, encodings[0], tolerance=0.5)
```

- dlib 单人单次耗时数十到数百毫秒，这里是**每人每 2 秒一次、串在主循环里**；多相机多人时帧率崩塌。
- `tolerance=0.5` 比库默认 0.6 更严格（误拒更高），属未记录的隐含参数。
- 人脸编码以 BLOB 存库后 `pickle.loads`（`backend.py:185`）——见 §7.4。

### 4.7 资源与清理

- 删除摄像头后，`frame_buffers[cam_id]`（`backend.py:1379`）、`person_states`（`796`）、`evidence_pending`（`810`）**都不会清理** → 内存泄漏，且已删除相机的残留状态仍可产生报警。
- `evidence_videos/` 现有 34 个条目（17 个帧目录 + 17 个 mp4，约 103MB），每条报警同时保存 JPEG 序列和 mp4，**没有保留期/容量策略**；`static/output/*.mp4`（各 15MB）同样如此。
- `error_log.txt`（`backend.py:937,1430`）逐帧追加、无轮转、无上限；当前文件里已有 **246 条** `'ThreadedCamera' object has no attribute 'get'` 记录（历史 bug，现已在 `backend.py:1385` 修正为 `cap.cap.get(...)`，但该日志文件被提交进了 git）。

---

## 5. 配置、依赖与“一条命令启动”全都对不上

### 5.1 `requirements.txt` 与实际 venv 差异（12 项）

| 包 | requirements.txt | `.venv` 实际 | 影响 |
| --- | --- | --- | --- |
| numpy | 1.26.4（注释强调必须 <2.0） | **2.2.6** | 与文件内说明直接冲突，ultralytics 兼容性风险 |
| torch | 2.4.1+cu124 | **2.14.0+cpu** | 无 CUDA（实测 `torch.cuda.is_available()=False`），README 的 GPU 预期不成立 |
| torchvision | 0.19.1+cu124 | 0.29.0 | 版本漂移 |
| ultralytics | 8.3.0 | 8.4.152 | API 差异面未知 |
| opencv-python / contrib | 4.11.0.86 | **5.0.0.93** | 主版本跨越，常量/API 有破坏性变更 |
| scikit-learn | 1.9.0 | 1.7.2 | 又与模型训练版本 1.6.1 不一致（三版本并存） |
| scipy / pandas / matplotlib | 1.17.1 / 3.0.5 / 3.11.1 | 1.15.3 / 2.3.3 / 3.10.9 | 对不上 |
| uvicorn / websockets | 0.52.4 / 17.1 | 0.53.0 / 16.1.1 | 对不上 |
| flask / imageio / seaborn / face_recognition | 未声明 | 未安装 | `app.py` 用 Flask、`video_processor.py:358` 用 imageio、README 提到 seaborn —— 均缺失 |
| pytest | 未声明 | 未安装 | `python -m pytest tests` 直接失败（只能 `python -m unittest discover -s tests`） |

Python 版本：README 写 3.9-3.11，venv 实际 **3.10.11**。

### 5.2 `start_system.bat` 指定的解释器不存在

```bat
:: start_system.bat:25-27
:: 2. Start Backend (in new window, using test02 virtualenv)
echo [2/3] Starting Backend Service (Python/FastAPI, venv: test02)...
start "Theft Guard Backend" cmd /k "cd /d "%PROJECT_DIR%" && "%PROJECT_DIR%test02\Scripts\python.exe" backend.py"
```

仓库里只有 `.venv`，**没有 `test02`**（`.gitignore` 里仍保留 `test02/`、`test03/` 的历史痕迹）。README 主推的“一键启动”必然失败。
同理 `clear_alerts.py:3` 与 `test_fix.py:2` 写死了 `os.chdir('E:/Theft-Detection-main')`，本机没有该目录。

### 5.3 阈值散落在 5 个地方且相互矛盾

| 阈值 | 值 | 位置 |
| --- | --- | --- |
| `THEFT_ALERT_THRESHOLD` | 0.7 | `detector_config.py:25`（被 `alert_policy` 使用） |
| `BEHAVIOR_ALERT_THRESHOLD` | 0.7 | `backend.py:710`（另一套，仅用于 `/api/analyze/*` 上色） |
| `BEHAVIOR_TIERS` + `_get_behavior_alert_threshold` | 0.7 / 0.85 / 1.0 | `backend.py:715-738`，**无任何调用方（死代码）** |
| `confidence_threshold` | 0.3 | `video_behavior.py:127,221` |
| `confidence_threshold` | 0.5 | `image_behavior.py:35,117` |
| `confidence_threshold`（JSON） | 0.15 | `detection_engine/config/detector_config.json` |
| 同类 JSON 副本 | 0.15，但 `retail_environment.enabled` 相反 | `src/config/detector_config.json`（false）vs `detection_engine/config/detector_config.json`（true） |

`detector_config.py` 的注释称“此前阈值散落三处，现已统一”，实际只统一了实时状态机部分，其余照旧。

### 5.4 死代码 / 死变量清单（后端）

| 名称 | 位置 | 状态 |
| --- | --- | --- |
| `clients` | `backend.py:460` | 全仓仅 1 次出现（定义），从未使用 |
| `roi_entry_times` / `last_alert_time`（全局） | `backend.py:453,455` | 残留；实际已改用 `cam_data[...]` 内字段 |
| `EVIDENCE_FPS` | `backend.py:800` | 定义后从未使用 |
| `BEHAVIOR_TIERS` / `_get_behavior_alert_threshold` / `NORMAL_BEHAVIORS` | `backend.py:715-738` | 无调用方 |
| `settings.showHeatmap` | `backend.py:131` | 无任何读取点 |
| `roi_points`（全局） | `backend.py:452` | 与相机级 `roi_points` 语义重叠 |
| `if True:` | `video_behavior.py:1211` | 注释“原条件为 object_count > 0”，硬编码开关 |
| `TODO: 添加更多规则检测` | `video_behavior.py:1213` | 全仓唯一 TODO |
| `'ret' in locals()` | `backend.py:969` | 自第一轮起恒为真，循环变量跨轮泄漏的典型写法 |

---

## 6. 工程结构：三套应用并存，两套已经死掉

### 6.1 `src/` 目录残缺导致两套入口必然 ImportError

当前 `src/` 下只有一个文件：`src/config/detector_config.json`。但代码里到处在 import 它：

| 位置 | 语句 | 结果 |
| --- | --- | --- |
| `detection_engine/ui/main_window.py:19` | `from src.models.detection import TheftDetector` | ImportError |
| `detection_engine/ui/main_window.py:20` | `from src.utils.video_processor import VideoProcessor` | ImportError |
| `detection_engine/ui/main_window.py:1074,1143,1562,1563` | 函数体内再次 import 同一批模块 | 运行时 ImportError |
| `detection_engine/app.py:30-32` | `from src.models.detection ...` 等 3 行 | ImportError（模块导入即崩） |

同时 `detection_engine/app.py:46` 声明 `template_folder='../templates'`，而仓库里**根本没有 `templates/` 目录**，`render_template('index.html')`（`app.py:90,95`）也无从渲染。`static/examples/`（`app.py:317`）同样不存在。

即：项目实际有 **3 个入口**

1. `backend.py` + Next.js（在用）
2. Flask `detection_engine/app.py`（导入即崩，且 Flask 未列入 requirements）
3. Tkinter `detection_engine/ui/main_window.py`（4273 行）+ `detection_engine/app/ui.py`（681 行，两套功能重叠的桌面 GUI，同样 import 失败）

后两者是纯死代码，却占用了约 5000 行维护面。

### 6.2 同一功能的多份实现

| 功能 | 实现 1 | 实现 2 | 实现 3 |
| --- | --- | --- | --- |
| 物体检测类 | `detection.py:1431` `ObjectDetector` | `object_detection.py:19` `ObjectDetector` | `common_detector.py:20` `CommonDetector` |
| 图像/视频入口 | `detection.py` `TheftDetector` | `image_detection.py` `ImageDetector` | `video_detection.py` `VideoDetector` |
| 零售环境判定 | `detection.py:203` `_is_retail_environment`（约 330 行） | `detection.py:537` `_is_retail_environment_video` | `common_detector.py:171,193` 两份简化版 |
| 行为检测 | `video_behavior.py`（4003 行） | `image_behavior.py`（2933 行） | `realtime_behavior.py`（302 行，实际生效） |
| 检测流水线 | `backend.py:889` `video_loop` | `offline_pipeline.py`（手抄 backend 逻辑） | `video_behavior.py:1291` `analyze_video_behavior` |
| `check_object_in_hand` | `backend.py:837` | `offline_pipeline.py:39`（注释直言“避免 import backend 拉起 FastAPI”） | — |

`offline_pipeline.py` 顶部注释自己承认是 `backend.py` 的复刻，且行号引用（第 969/996/1100 行）已随改动漂移——**两份逻辑必然继续分叉**（例如 `offline_pipeline.py:191` 的持物判定窗口写死 3 秒，而 backend 里的持物持久化逻辑是另一套），用它调出来的参数不一定适用于线上。

### 6.3 文件与函数粒度

- `main_window.py` 4273 行、`video_behavior.py` 4003 行、`image_behavior.py` 2933 行、`detection.py` 1836 行均违反任何合理的单文件规模上限。
- 函数级：`analyze_video_behavior`（`video_behavior.py:1291-1818`，约 520 行）、`process_frame`（`3103-3245`）、`video_loop`（`backend.py:889-1432`，约 540 行）、`analyze_behavior`（`main_window.py:2014-2149`）等属于“超级函数”。
- 无条件/死分支：`video_behavior.py:1211` 的 `if True:`。

### 6.4 异常处理：9 处裸 `except:`，大量异常被静默吞掉

| 位置 | 写法 | 后果 |
| --- | --- | --- |
| `backend.py:152` | `except: pass` | 热力图越界静默忽略（可接受，但无日志） |
| `backend.py:379-380, 444-447` | `except Exception: pass` | 证据流/删除告警失败无痕迹 |
| `backend.py:663-670` | `except:` → `random.randint()` | psutil 失败时**返回随机数冒充系统负载** |
| `backend.py:1026-1027` | `except:` | 关键点提取失败静默置空 |
| `main_window.py` 2 处、`image_behavior.py` 2 处 | `except:` | 无日志 |
| `detection.py:1063-1065` | 捕获后仅 warning + 回退 | 见 §3.2，掩盖了模型完全不可用 |

另外 147 处 `print()` 调试输出（`backend.py` 51 处、`diag_engine.py` 41 处）替代了日志系统，生产环境无法按级别过滤/落盘；而 `logs/video_behavior.log` 已经被撑到 105KB，里面反复出现同一批 ERROR。

---

## 7. 安全与隐私问题

### 7.1 明文凭据已进入版本库

```jsonc
// cameras.json:1-19  （该文件已被 git 跟踪）
[{"id":"e584...","name":"test1","source":"rtsp://admin:a12345678@172.30.20.64:554/Streaming/Channels/102", ...},
 {"id":"74be...","name":"test2","source":"rtsp://admin:a12345678@172.30.20.63:554/Streaming/Channels/102", ...},
 {"id":"f025...","name":"li",   "source":"rtsp://admin:a12345678@172.30.20.64:554/Streaming/Channels/102", ...}]
```

```python
# diag_engine.py:49
RTSP_URL = "rtsp://admin:a12345678@172.30.20.63:554/Streaming/Channels/102"
```

- 摄像头账号 `admin:a12345678` + 内网 IP 已随仓库分发；即使删除文件，**git 历史里仍然存在**。
- `.gitignore` **没有忽略 `.env`**，而 `save_settings`（`backend.py:263-271`）会 `set_key(".env", "SMTP_PASSWORD", ...)` / `TELEGRAM_BOT_TOKEN` 写入 `.env` → 用户配一次邮箱就会把邮箱密码生成到工作区，一次 `git add .` 即泄露。
- `settings.json` 被 git 跟踪（内容随用户配置变化），`cameras.json` 同理 → 配置漂移 + 泄露通道。

### 7.2 接口无鉴权 + 监听全网

| 项 | 现状 | 风险 |
| --- | --- | --- |
| 认证 | 完全没有（无 token/密码/会话） | 局域网内任何人可看所有摄像头、删告警、删摄像头、上传文件触发推理 |
| 监听 | `backend.py:1809` `uvicorn.run(app, host="0.0.0.0", port=8000)` | 暴露到所有网卡 |
| CORS | `backend.py:76-82` 只允许 `http://localhost:3000` | 与前端默认 `127.0.0.1:8000`（`dashboard/src/app/page.tsx:19`）不一致；用 `127.0.0.1:3000` 打开面板时所有接口被 CORS 拦掉 |
| WebSocket | `backend.py:1606` `/ws` 无任何校验 | 任意客户端可拉取全部实时画面 |
| 消息泄露 | `GET /settings`（`backend.py:253-255`）直接返回内存中的 `current_settings` | 包含 `senderPassword`、`telegramBotToken` 字段（落盘时虽然清空，但接口未脱敏） |
| 静态目录 | `backend.py:69-70` 挂载 `/alerts`、`/evidence_videos` | 任何人可直接列/取证据图片与视频 |

### 7.3 上传与文件处理

- `POST /faces/register`（`backend.py:206`）、`POST /api/analyze/image`（`1655`）、`POST /api/analyze/video`（`1726`）均**无大小限制、无类型校验、无内容校验**，直接 `write(await file.read())` 落盘。
- 临时文件写在 `alerts/` 内（`backend.py:1661,1732`），而 `alerts/` 是静态目录 → 处理期间文件可被 `/alerts/xxx` 直接访问。
- `/api/analyze/video` 的 `pose.track()` 逐帧处理上传视频，等于把任意耗时任务暴露给未认证调用者（配合 §4.1 可做拒绝服务）。

### 7.4 反序列化 = 潜在远程代码执行

| 位置 | 调用 |
| --- | --- |
| `backend.py:185` | `pickle.loads(encoding_blob)`（来自 SQLite，可经 `/faces/register` 间接写入） |
| `video_behavior.py:508,532` | `pickle.load(open(...,'rb'))` |
| `image_behavior.py`（3 处） | `pickle.load` |
| `image_detection.py`（2 处） | `pickle.load` |
| `detection.py:127` | `joblib.load(model_path)`（joblib 同样是 pickle） |
| `common_detector.py`、`detection.py:1470` | `torch.load(path)`（未指定 `weights_only=True`） |

任何人替换模型文件或写入 faces 表即可执行任意代码；`torch.load` 未启用 `weights_only` 是明确的安全建议违反项。

### 7.5 路径与文件系统

- `GET /api/evidence/stream/{alert_id}`（`backend.py:361-368`）直接把 `alert_id` 拼进路径 `f"{EVIDENCE_DIR}/{alert_id}"`，**未校验是否为 UUID**；形如 `..%2F..%2FsomeDir` 的路径参数可被解码后做目录遍历，读取任意含 `.jpg` 的目录（并自定义 `Content-Length` 头，具备响应头注入的雏形）。建议：`uuid.UUID(alert_id)` 校验 + 路径归一化后再判断前缀。
- `DELETE /history/{alert_id}`（`backend.py:402-447`）的 `image_path` / `video_path` 直接取自数据库并用于 `os.remove` / `shutil.rmtree`，若历史数据被污染则可删除任意路径；同时存在 §4.2 的“删光 mp4”问题。
- `find`/`delete_camera` 无归属校验，任何调用者都能删任意相机。

---

## 8. 交付与仓库卫生

### 8.1 企业 DLP 加密了 38 个关键文件（`%TSD-Header-###%`）

这些文件在工作区里是**密文**，只有白名单进程（本机的 `python.exe`、`git.exe`）能透明解密。用 PowerShell 直接读会得到乱码，用 CI / Docker / WSL / 另一台机器打开就是一团二进制。

| 类别 | 文件 |
| --- | --- |
| 依赖与许可 | `requirements.txt`、`LICENSE`、`.env.example`、`start_system.bat` |
| 全部前端源码 | `dashboard/src/app/**`（7 个页面 + layout + globals.css + favicon）、`dashboard/src/components/*.tsx`、`dashboard/next-env.d.ts`、`dashboard/public/*.svg` |
| 行为引擎 | `detection_engine/models/behavior/image_behavior.py` |
| 模型权重（全部） | `yolov8n.pt`、`yolov8n-pose.pt`、`models/yolov8n.pt`、`models/yolov11n.pt`、`models/theft_xgb_model.pkl`、`models/standard/theft_xgb_model.pkl`、`models/yolov8n-pose.pt.*.partial` |
| 运行产物 | `static/output/*.mp4`、`diag_frame_0.jpg`、`error_log.txt`、`docs/*.png` |

**影响**：

- 别人 clone 仓库后 `pip install -r requirements.txt` 会读到密文（在无该 DLP 的机器上），**装不了依赖**；
- 前端源码在非白名单工具里不可读，Code 评审、CI 构建、Docker 镜像构建全部会失败；
- 模型权重被加密，任何容器化/服务器部署都会因“文件损坏”而加载失败；
- 对比之下 `backend.py`、`realtime_behavior.py`、`alert_policy.py`、`tests/`、`tools/` 等**本机新建/修改过的文件是明文**——说明这是“从压缩包解出的文件被自动加密”的策略副作用。建议对项目目录申请加密豁免，或至少把上述文件重新以明文方式纳入交付。

### 8.2 提交进 git 的产物与冗余文件

当前跟踪 79 个文件、合计 **59.3MB**（`.git` 目录 38.9MB）：

| 文件 | 大小 | 问题 |
| --- | --- | --- |
| `static/output/result_*.mp4` ×2 | 各 15MB | 演示输出视频，不应入库 |
| `yolov8n.pt`（根） | 6.5MB | 与 `models/yolov8n.pt` 重复 |
| `yolov8n-pose.pt`（根） | 6.8MB | 与 `.venv`/推理所需路径不一致，且无 `models/` 副本命名统一 |
| `models/yolov11n.pt` | 5.6MB | 与根目录模型职责重叠 |
| `models/theft_xgb_model.pkl` + `models/standard/theft_xgb_model.pkl` | 各 1.4MB | 两份内容完全相同 |
| `models/yolov8n-pose.pt.9b6795a2bc63401b8579dcc889ebd762.partial` | 659KB | **下载中断残留**，被误提交 |
| `error_log.txt` | 28KB | 运行日志 |
| `diag_frame_0.jpg` | 90KB | 调试截图 |
| `theftguard.db` | 0 字节 | **空数据库**，代码只用 `theft_detection.db`，但 `clear_alerts.py` 还在遍历它 |
| 根目录 `package-lock.json` | 105 字节 | 与项目无关的 stub（真正的锁文件在 `dashboard/package-lock.json`） |

另：`evidence_videos/`（103MB）、`alerts/`（29 张截图）、`tmp_eval_frames/`、`logs/`、`.venv/`、`.idea/`、`.zcode/` 未被跟踪（`.gitignore` 覆盖了大部分），但**仍在工作区**里随项目一起分发。

### 8.3 其他工程化缺失

- 没有 `pyproject.toml` / `setup.py` / `setup.cfg`，包结构（`detection_engine`）不可安装，只能靠 `sys.path.insert` 硬塞（`backend.py:20-22`、`main_window.py:17`、`app.py:24-27`）。
- 没有 CI（无 `.github/workflows`）、没有 lint/format 配置（无 `ruff`/`black`/`.flake8`）、没有 pre-commit。
- 没有 `Dockerfile` / `docker-compose.yml`，部署方式只有“Windows 上双击 bat”。
- 没有 `.editorconfig`，中英土三语混排的注释（`backend.py:892` `"Video Loop Başlatılıyor..."`、`590` `"Kamera eklendi"`、`943` `"SINYAL YOK"`、`1326` `"RESTRICTED AREA ENT!"`）说明代码是从另一项目 fork 后局部汉化，风格不统一。

---

## 9. 前端（Next.js）问题

### 9.1 展示假数据

```tsx
// dashboard/src/app/page.tsx:44
const load = activeCount > 0 ? Math.floor(30 + Math.random() * 20 + (activeCount * 5)) : 10;
```

“系统负载”是**随机数**，与后端 `/stats` 返回的 `cpu_load`/`ram_load` 无关（`StatsCharts.tsx:30` 才真正调 `/stats`）→ 首页指标不可信。

### 9.2 告警徽标正则与后端消息格式不匹配

```tsx
// dashboard/src/components/CameraGrid.tsx:156
const m = alertMessage.match(/^BEHAVIOR:\s*(.+?)\s*\(([0-9.]+)\)$/);
```

后端实际消息是 `THEFT: rapid_item_concealment | probability=0.85 | sequence=Y | corroborated=holding`、`RESTRICTED AREA INTRUSION`、`BLACKLIST FACE: xxx` 等，**没有一种形如 `BEHAVIOR: xxx (0.85)`** → 永远走 fallback 分支，把整条原始消息（含英文技术细节）直接怼到用户界面上。

### 9.3 API 基址与环境变量不统一

| 文件 | 写法 |
| --- | --- |
| `page.tsx:19`、`StatsCharts.tsx:30` | `… \|\| "http://127.0.0.1:8000"` |
| `analyze/page.tsx:31`、`cameras/page.tsx:36`、`faces/page.tsx:24`、`history/page.tsx:26` | `… \|\| "http://localhost:8000"` |
| `settings/page.tsx:26,55` | `process.env.NEXT_PUBLIC_API_URL` **无兜底** |
| `next.config.ts` | 注入默认值 `http://localhost:8000` |

- `localhost` 与 `127.0.0.1` 混用：若浏览器把 `localhost` 解析成 `::1` 而后端只监听 IPv4，`/cameras` 等请求会失败（README 自己也建议改用 127.0.0.1）。
- `settings/page.tsx` 依赖 `next.config.ts` 的注入才不崩，单独跑测试或换构建方式就成 `undefined/settings`。
- 后端 CORS 只放行 `localhost:3000`，与前端 127 系列基址存在组合失败面。
- `dashboard/.env.local` 不存在（README 排障章节却要求它存在）。

### 9.4 WebSocket 相关

- `CameraGrid.tsx:66-124` 与 `cameras/page.tsx:56-89` **各自新建一个 WebSocket**，页面级无复用；切换页面会重建连接，且都无鉴权、无背压、无消息节流。
- 后端每 40ms 向每个客户端推送**全部相机的 base64 JPEG**（`backend.py:1614-1621`）；2 路 720p 时约 4-6MB/s，前端 `JSON.parse` + `setState` 同频触发重渲染，浏览器负载高。
- `CameraGrid.tsx:71` 用 `replace("http","ws")` 推导 ws 地址，遇 `https` 恰好得到 `wss`（可用），但对自定义域名/端口写法脆弱。

### 9.5 其他 UI/交互缺陷

| 位置 | 问题 |
| --- | --- |
| `history/page.tsx:97-103` | CSV 导出用 `data:text/csv` URI + `encodeURI`，行数多或含特殊字符时会被浏览器截断/报 URI 过长；应改 `Blob` + `URL.createObjectURL` |
| `history/page.tsx:119-122` | “最近7天”按钮**没有 onClick**，纯装饰 |
| `history/page.tsx:73-79` | 事件分类靠 `message.includes("BEHAVIOR:")` 等字符串匹配，与 §9.2 同源问题 |
| `cameras/page.tsx:449-450` | ROI 画布固定 `width=1280 height=720`，非 16:9 摄像头画面会被拉伸变形（`backend.py` 侧也硬编码 1280x720） |
| `cameras/page.tsx:192`、`248,252` | 删除用原生 `confirm()`、错误用原生 `alert()`，与整体 glassmorphism 风格割裂 |
| `faces/page.tsx`、`analyze/page.tsx` | 大量 `any` 类型（`page.tsx:26,34` 等），TypeScript 严格模式形同虚设；无错误边界 |
| 全局 | 无轮询取消/session 超时处理；多个页面 `useEffect` 缺依赖（ESLint 会告警 `react-hooks/exhaustive-deps`） |

### 9.6 `dashboard/AGENTS.md` 的提醒值得注意

```
<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know
This version has breaking changes — APIs, conventions, and file structure may all differ from your training data.
```

配合 `package.json`（Next **16.2.6**、React **19.2.4**、Tailwind **4**、ESLint 9）来看，README 里写的 “Next.js 14+ / React 18 / Tailwind CSS” 已经完全对不上；团队若按旧文档改代码会踩坑。

---

## 10. 测试与评估体系

### 10.1 测试现状

- 唯一测试文件：`tests/test_alert_policy.py`（10 个用例）。实测结果：
  ```
  .\.venv\Scripts\python.exe -m unittest discover -s tests -v
  → Ran 10 tests ... OK
  ```
- `python -m pytest tests` **直接失败**（`No module named pytest`，requirements 未声明）。
- 覆盖范围仅限 `alert_policy.py` 与 `detector_config.py` 的纯函数；`realtime_behavior.py` 的状态机、`offline_pipeline.py`、`backend.py` 的两条报警通路、证据编码、ROI/徘徊逻辑**零覆盖**。
- 没有夹具、没有 CI 挂钩、`tests/__init__.py` 为空。
- 所谓“验证安装”的脚本是三个 ad-hoc 打印脚本：`test_setup.py`（跑一张全黑图）、`test_pose.py`（只加载模型）、`diag_engine.py`（连真机 RTSP 打 60 帧）。它们用 `print("SETUP_SUCCESS")` 之类的字符串表达结果，无法断言、无法自动化，且 `test_fix.py` 里写死了本机不存在的路径。

### 10.2 评估体系缺失（这是“精度优化”无法推进的根本原因）

- 无 ground truth、无 precision/recall 脚本、无基线报告。
- `.zcode/plans/plan-sess_*.md` 里规划了 `tools/eval_detection.py`（精度/查全两种模式）与 `tools/train_shoplifting.py`，但**这两个文件不存在**；而 `tools/export_alerts_for_labeling.py:20,99` 在输出里直接引导用户去运行不存在的 `tools/eval_detection.py`。
- `labels/alerts_to_label.csv` 已生成但 `is_true_positive` 全空；且如 §3.1 所述，这批数据来自旧策略（含大量 `looking_around`），即使标注完也不能直接代表当前系统的表现。
- `offline_pipeline.py` 是唯一的回放评估工具，它复刻的是 backend 逻辑而非调用 backend，评估结论与线上行为可能不一致。
- 计划文档里提到的其他项（阈值网格搜索、`shoplifting.pt` 微调脚本、`model_complexity` 升 1）均未落地；`detector_config.apply_overrides()`（JSON 调参入口）虽然实现了，但仓库里**没有 `detector_tuning.json`**，也无人使用。

---

## 11. README 与实际不符的清单

| README 说法 | 实际情况 | 位置 |
| --- | --- | --- |
| “一键 `start_system.bat` 启动前后端” | 后端用 `test02\Scripts\python.exe`，该 venv 不存在 | `start_system.bat:27` |
| “Next.js 14+、React 18、Tailwind CSS” | Next 16.2.6、React 19.2.4、Tailwind 4 | `dashboard/package.json` |
| 提供 Live Demo（Vercel 链接） | 后端必须在本机跑（本地 SQLite + 本地摄像头），线上页面拿到的是空数据/连接失败 | README 顶部 |
| “放置 `shoplifting.pt` 会自动升级为专用模型” | 该文件不存在；即便存在，报警链路仍只看 `rapid_item_concealment` 状态机 | `backend.py:900-912` |
| “Activity Heatmaps 活动热力图” | 热力图被 `plot()` 覆盖，且 `showHeatmap` 设置从未生效 | §3.5 |
| “偷盗概率标注” | 同上，概率文字被覆盖，实时画面看不到 | §3.5 |
| “XGBoost 行为分类 / ML Detected Theft” | ML 通路 100% 异常回退，该标签永不产生 | §3.2 |
| “3 秒智能告警冷却” | `ALERT_COOLDOWN` 只用于专用模型分支（`backend.py:994`）；其余告警靠“每人一次”布尔量去重 | `backend.py:994` vs `1328-1330` |
| “多线程异步读帧避免卡顿” | 无丢帧策略，延迟持续累积；无重连 | §4.4 |
| 排障要求 `dashboard/.env.local` 存在 | 该文件不存在（靠 `next.config.ts` 注入默认值） | `dashboard/` |
| `dashboard/README.md` 指引改 `app/page.tsx` | 实际路径是 `src/app/page.tsx`（还是 create-next-app 模板） | `dashboard/README.md:19` |
| “人脸识别面板/黑名单自动报警” | `face_recognition` 未安装（Windows 难装）；代码有降级，但 README 把它当既有能力宣传 | `backend.py:60-64` |

---

## 12. 修复优先级路线图（建议）

### P0（当天，必须）

1. **修死锁**：`save_cameras()` 拆出 `_save_cameras_unlocked()`；或把 `CameraManager.lock` 换成 `RLock`。同时在 `save_camera_roi` 里“先改内存、后落盘”。（`backend.py:544-559, 688-698`）
2. **修删除逻辑**：`backend.py:437-441` 改为按 `alert_id` 前缀匹配删除对应 mp4，禁止全目录遍历删除。
3. **修文件名冲突**：`backend.py:1440` 时间戳加毫秒或直接用 `alert_id` 命名截图。
4. **明确模型策略**：要么把 6 分类模型按行为分类正确接好（30 维特征 + `label_encoder` + 取对应类别概率），要么删掉这条“伪 ML 通路”和 README 相关宣传，避免继续误导。

### P1（一周内）

5. **统一报警策略与阈值**：删除 `backend.py:715-738` 死代码与 `BEHAVIOR_ALERT_THRESHOLD`，所有阈值只留 `detector_config.py`（或 `detector_tuning.json`）；同步 `video_behavior.py:127`、`image_behavior.py:35`、两份 JSON。
6. **修标注覆盖**：`backend.py:1360-1367` 改成在 `plot()` 之后绘制（或不用 `plot()`，自己画骨架），并让 `showHeatmap` 真正生效。
7. **修坐标系**：聚合通路要么传入全帧图 + 全帧检测结果，要么把 `results_pose[0]` 按裁剪偏移平移后再传入。
8. **接口不阻塞事件循环**：模型全局单例；`analyze_image/video` 改 `run_in_threadpool` 或独立 worker + 任务 ID 轮询。
9. **安全底线**：删除 `cameras.json` 中的明文凭据并改用环境变量/凭据库（同时清理 git 历史）；`.gitignore` 加 `.env`、`*.pkl`、`*.pt`；`GET /settings` 脱敏；给所有写接口加最小鉴权；`host` 改为 `127.0.0.1` 或加反代鉴权。
10. **仓库瘦身**：`git rm --cached` 掉模型副本/partial/mp4/日志/空 db/stub lock；模型改由脚本或 Git LFS 管理。

### P2（一个月内）

11. **删死代码**：移除 `detection_engine/app.py`、`detection_engine/app/ui.py`、`detection_engine/ui/main_window.py`（或补回 `src/` 并纳管），清理 `clients`、`EVIDENCE_FPS`、全局 `roi_points/roi_entry_times/last_alert_time`。
12. **补评估**：实现 `tools/eval_detection.py`（精度 + 查全），产出基线报告；把 `labels/alerts_to_label.csv` 重新按当前策略采一遍样本。
13. **补测试**：为 `realtime_behavior` 状态机（给定关键点序列断言事件）、`offline_pipeline`、证据编码、ROI/徘徊逻辑加单测；把 `pytest` 写进 requirements 并接 CI。
14. **前端修整**：去掉 `Math.random` 假负载、修告警徽标解析、统一 API 基址常量、CSV 改 Blob 导出、去掉死按钮。
15. **一致性改造**：所有路径改为基于 `__file__` 的绝对路径（解决 §5.2、日志里“模型文件不存在”但文件实际存在这类 CWD 相关问题）；日志改 `logging` 并加轮转；`error_log.txt` 移出仓库。

---

## 13. 附录：证据与可复现命令

### 13.1 本报告用到的复现命令

```powershell
# 1) 单元测试（通过）
.\.venv\Scripts\python.exe -m unittest discover -s tests -v      # → Ran 10 tests, OK

# 2) 模型真实结构（30 维 6 分类）
.\.venv\Scripts\python.exe -c "import pickle;d=pickle.load(open('models/theft_xgb_model.pkl','rb'));m=d['model'];print(m.n_features_in_, m.classes_)"
# → 30 [0 1 2 3 4 5]

# 3) 运行环境
.\.venv\Scripts\python.exe -c "import sys,cv2,numpy,torch,mediapipe;print(sys.version);print(cv2.__version__, numpy.__version__, torch.__version__, torch.cuda.is_available())"
# → 3.10.11 / 5.0.0 2.2.6 2.14.0+cpu False

# 4) 依赖差异
.\.venv\Scripts\python.exe -m pip list --format=freeze   # 与 requirements.txt 对比

# 5) 报警策略与历史数据冲突（31 条：17 crouching + 11 looking_around + 3 arm）
.\.venv\Scripts\python.exe -c "import sqlite3;[print(r) for r in sqlite3.connect('theft_detection.db').execute('select behavior_type,count(*) from alerts group by 1')]"

# 6) 死锁复现（沙箱内测试，cwd 切到临时目录，不修改仓库）
#    构造 CameraManager 后 await save_camera_roi(...) → 进程永久挂起，需 kill

# 7) DLP 加密文件清单（38 个）
Get-ChildItem -Recurse -File | ForEach-Object { try { $b=New-Object byte[] 16; $fs=[System.IO.File]::OpenRead($_.FullName); $fs.Read($b,0,16)|Out-Null; $fs.Close(); if([Text.Encoding]::ASCII.GetString($b) -eq '%TSD-Header-###%'){$_.FullName} } catch {} }
```

### 13.2 关键证据摘录

**`error_log.txt`（246 条同一错误，历史 bug 残留）**
```
2026-09-11 17:37:11.965210: Loop Runtime Error: 'ThreadedCamera' object has no attribute 'get'
（连续 246 次，每次间隔约 1 秒）
```

**`logs/video_behavior.log`（105KB，反复出现的初始化失败）**
```
2026-09-15 13:43:49,593 - video_behavior_detector - INFO  - 视频行为检测器初始化完成
2026-09-15 13:43:49,593 - video_behavior_detector - ERROR - 初始化姿态检测器时出错: module 'mediapipe' has no attribute 'solutions'
2026-09-15 13:43:49,599 - video_behavior_detector - WARNING - 标准XGBoost模型文件不存在: models\standard\theft_xgb_model.pkl
2026-09-15 13:43:49,601 - video_behavior_detector - WARNING - 行为分类器模型文件不存在: models/theft_xgb_model.pkl
```
> 注：当前环境已复核 `mediapipe 0.10.14` 的 `hasattr(mp,'solutions') == True`、两个 pkl 也都存在 —— 说明当时的失败来自**工作目录/环境不一致**（代码到处使用相对路径），这正是 §5.2 / §12-15 要修的问题。

**`theft_detection.db` 样本（同秒重复 + 低风险行为）**
```
20260915_164734 | suspicious_crouching | 0.70 | THEFT: suspicious_crouching | 概率=0.70 | rule=1.00 continuity=0.00 sequence=N
20260915_164734 | looking_around       | 0.63 | THEFT: looking_around | 概率=0.63 | rule=0.69 continuity=0.15 sequence=N
20260915_173140 | suspicious_crouching | 0.75 | THEFT: looking_around | 概率=0.70 | rule=1.00 continuity=0.00 sequence=N
20260915_173140 | suspicious_crouching | 0.80 | THEFT: suspicious_crouching | 概率=0.75 | rule=1.00 continuity=0.05 sequence=N
```
> 注意 20260915_164346 行：`behavior_type` 与消息里的行为名不一致（`looking_around` vs `THEFT: unusual_arm_position`）；20260915_173140 两条同为 `suspicious_crouching` 却一条消息写 `looking_around` —— 说明 `top_behavior` 的赋值与消息拼装不同步（`backend.py:1274-1307`）。

### 13.3 工作区状态（分析时）

```
 M backend.py
 M detection_engine/realtime_behavior.py
 ?? .zcode/  detection_engine/alert_policy.py  detection_engine/detector_config.py
 ?? detection_engine/offline_pipeline.py  labels/  tests/  tmp_eval_frames/  tools/
```

即：`alert_policy.py`、`detector_config.py`、`offline_pipeline.py`、`tests/`、`tools/`、`labels/` 都是**未提交的新增文件**，本轮的“阈值集中化 + 佐证确认 + 评估导出”改造尚未落库；同时 `backend.py`、`realtime_behavior.py` 有未提交改动 —— 与 §3.1 中“历史数据由旧策略产出”相互印证（策略改动只在工作区生效，数据库里还是旧数据）。

---

## 14. 一句话总结

这套系统的**主链路目前是“几何状态机 + 手写规则”**，XGBoost 与 21 种行为引擎基本属于装饰；而**摄像头配置这一必经操作会直接死锁整个后端**，实时画面上的核心可视化（概率、热力图、物品框）被一行 `plot()` 覆盖掉了，删除一条告警会清空所有证据视频，仓库里还躺着明文摄像头密码与被 DLP 加密的依赖清单和全部前端源码。在修掉 P0/P1 之前，不建议用于任何真实场景的取证或对外演示。
