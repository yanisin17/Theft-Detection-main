# TheftGuard AI 问题修复报告

> 修复依据：`codex-项目分析.md` 中指出的 8 个识别不准原因与高优先级安全问题
> 修复日期：2026-09-18
> 验证状态：14/14 单元测试通过，5 个 Python 文件语法检查通过，前端 tsc 编译通过

---

## 一、采集链路修复（对应问题 #1、#2）

**文件：`backend.py`**

### 1.1 ThreadedCamera 完全重写

| 修复项 | 修复前 | 修复后 |
|--------|--------|--------|
| 读取方式 | 串行 `cap.read()` 阻塞整个循环 | 每路摄像头独立采集线程，只保留最新帧 |
| 断线处理 | 无重连，断流后画面永久卡死 | 连续 15 次读取失败触发重连，指数退避 2s→30s |
| RTSP 传输 | 默认 UDP，易丢包花屏 | `OPENCV_FFMPEG_CAPTURE_OPTIONS` 强制 TCP + 5 秒 socket 超时 |
| 运行统计 | 无 | `stats` 字典记录宽高/FPS/读取帧数/丢弃帧数/读取延迟/重连次数/状态 |
| 帧时间戳 | 无 | `timestamp()` 返回最近帧采集墙钟时间 |

### 1.2 凭据脱敏机制

新增 `_expand_env_source()`：摄像头 source 支持 `${VAR}` 环境变量占位符（如 `rtsp://${CAM_USER}:${CAM_PASS}@ip:554/...`），账号密码不再明文写进 cameras.json。占位符仅在 `ThreadedCamera` 内部展开，落盘的仍是占位符形式。

### 1.3 恢复的回归缺陷

修复过程中发现工作区存在三处未提交的破坏性改动，已全部恢复：

- **RLock 死锁**：`CameraManager` 的锁被改回 `Lock`，导致 `save_camera_roi` 持锁调用 `save_cameras` 时嵌套获取同一把锁而死锁 → 恢复 `threading.RLock()`
- **危险删除**：`_find_evidence_mp4`（按告警文件名精确定位对应证据视频）被替换为遍历删除 `evidence_videos/` 下**所有** mp4 的代码 → 恢复安全版本
- **ROI 落盘**：`save_camera_roi` 在持锁状态下写文件 → 恢复锁外落盘（`found` 标志 + 锁外 `save_cameras()`）

---

## 二、检测与显示修复（对应问题 #4、#5、#8）

**文件：`backend.py` video_loop**

### 2.1 绘制顺序（问题 #8）

修复前：`results_pose[0].plot()` 在最后执行，把热力图、行为文字、告警红框全部覆盖。
修复后：

```python
clean = frame.copy()                      # 推理始终用未标注的原始帧
results_pose = model_pose.track(clean, ...)
frame = results_pose[0].plot()            # 先渲染 YOLO 骨架
frame = get_heatmap_overlay(...)          # 再叠加自定义标注
```

### 2.2 裁剪坐标系偏移（问题 #5，核心修复）

修复前：`sub_frame` 是人物裁剪图，但传入聚合引擎的物体框仍是整帧坐标，导致物体-人体关联完全错位。
修复后：物体框统一平移到裁剪图坐标系：

```python
shifted_detections.append({
    'bbox': [int(b[0] - sx1), int(b[1] - sy1),
             int(b[2] - sx1), int(b[3] - sy1)],
})
```

附带发现：旧代码对 ultralytics `Results` 的属性访问（`detections.xyxy`）在新版中从未生效过，改为传 list 格式后物体关联逻辑**首次真正生效**。

### 2.3 过期物体框（问题 #4 部分）

物体检测每 5 帧执行一次，其余帧复用旧结果。修复后超过 1 秒的 `last_objects` 自动清空，不再把过期框当作"手中物品"参与判定。人脸裁剪、行为裁剪均改用 `clean` 帧。

### 2.4 报警置信度读取回归

`realtime_behavior.py` 的藏匿事件把 `confidence` 键改成了 `alert_score`（0.45~0.85 报警分），但 backend 仍读 `confidence`，导致**报警永远不触发**。修复为兼容读取：

```python
event_confidence = float(realtime_event.get('alert_score',
                                            realtime_event.get('confidence', 0.0)))
```

---

## 三、规则检测器关键点索引修复（对应问题 #6 关联缺陷）

**文件：`detection_engine/models/behavior/video_behavior.py`**

### 问题根源

6 个规则子检测器用 **COCO 17 点索引**读取 **MediaPipe 33 点**数据——肩膀索引 (5/6) 实际读到的是**眼睛**，手肘 (7/8) 读到的是**耳朵**，全部行为规则判定基于错误的关键点。

### 索引映射修复表

| 部位 | 错误索引 (COCO) | 正确索引 (MediaPipe) |
|------|-----------------|---------------------|
| 左/右肩 | 5 / 6 | 11 / 12 |
| 左/右肘 | 7 / 8 | 13 / 14 |
| 左/右腕 | 9 / 10 | 15 / 16 |
| 左/右髋 | 11 / 12 | 23 / 24 |
| 左/右膝 | 13 / 14 | 25 / 26 |
| 左/右耳 | 3 / 4 | 7 / 8 |

涉及的检测函数：`_detect_abnormal_arm_positions`、`_detect_hands_behind_back`、`_detect_unusual_reaching`、`_detect_suspicious_crouching`、`_detect_body_shielding`、`_detect_abnormal_head_movement`、`_detect_item_grabbing_and_concealment`。

各函数的长度守卫同步更新（如肩肘腕检查 `< 11` → `< 17`，含髋检查 `< 15` → `< 25`），并经 Grep 全文验证无残留旧索引。

---

## 四、前端修复（对应问题 #7）

### 4.1 ROI 画布分辨率（`dashboard/src/app/cameras/page.tsx`）

修复前：画布固定 `width={1280} height={720}`，与实际视频分辨率不一致导致 ROI 点击坐标系统性偏移。
修复后：后端在 WS 帧消息中新增 `width`/`height` 字段（实际视频宽高），画布动态使用该尺寸建立坐标系。

### 4.2 告警标签解析（`dashboard/src/components/CameraGrid.tsx`）

修复前：用正则 `^BEHAVIOR:\s*(.+?)\s*\(([0-9.]+)\)$` 匹配告警消息，与后端实际格式**永远不匹配**，行为类型和置信度从未显示过。
修复后：直接使用后端 `alert_payload` 中的结构化字段 `behavior_type` / `confidence`，并增加 13 种行为类型的中文标签映射（如 `rapid_item_concealment` → "快速藏匿物品"）。

### 4.3 历史时间格式（`dashboard/src/app/history/page.tsx`）

修复前：`formatTime` 假设时间戳固定 15 位，微秒化后（22 位）解析失败直接显示原始字符串。
修复后：正则匹配同时兼容 `YYYYMMDD_HHMMSS`（15 位）与 `YYYYMMDD_HHMMSS_ffffff`（22 位）。

---

## 五、安全修复（高优先级问题）

| 修复项 | 修复前 | 修复后 |
|--------|--------|--------|
| 凭据明文 | cameras.json 含 `rtsp://admin:a12345678@...` 且被 git 跟踪 | 改为 `${CAM_USER}`/`${CAM_PASS}` 占位符；`git rm --cached` 移出索引；`.gitignore` 增加 `cameras.json` 和 `.env` |
| 凭据存放 | 无 | 真实凭据移入本地 `.env`（不入库），`.env.example` 提供模板 |
| 网络暴露 | uvicorn 监听 `0.0.0.0`（全网卡可达，无认证） | 默认 `127.0.0.1`，通过 `HOST` 环境变量可覆盖 |
| 文件覆盖 | 告警文件名精确到秒，同一秒多个告警互相覆盖截图/证据 | `%Y%m%d_%H%M%S_%f` 微秒精度，不再覆盖 |
| 示例配置 | 无 | 新增 `cameras.json.example` 模板（且示例改用主码流 `Channels/101`） |

---

## 六、验证结果

| 验证项 | 结果 |
|--------|------|
| `py_compile` 语法检查（backend.py、video_behavior.py、realtime_behavior.py、alert_policy.py、detector_config.py） | 全部通过 |
| 单元测试（`.venv` 下 `python -m unittest discover -s tests`） | **14/14 通过** |
| backend 完整导入（含 cameras.json 加载、占位符展开、MediaPipe 初始化） | 成功 |
| 前端 `npx tsc --noEmit` | 通过 |

覆盖的回归测试包括：报警策略序列确认、藏匿事件 alert_score 区间、告警文件名微秒格式、plot 后标注保留。

---

## 七、遗留建议（未在本次修复范围内）

1. **模型能力**：当前仍是通用 COCO 物体检测模型（`shoplifting.pt` 不存在），只能检测"手中是否持物"，不能判断"偷盗"——需按分析文档第三阶段重新训练/收集真实数据集（`labels/`、`tmp_eval_frames/` 已有基础）。
2. **git 历史泄露**：旧明文密码仍在 git 历史中，如仓库已推送远端，建议直接**更换摄像头密码**；如需清除历史记录需 `git filter-repo`（破坏性操作）。
3. **子码流分辨率**：`Channels/102` 子码流分辨率不稳定，生产环境建议切换主码流 `Channels/101`。
4. **评估体系**：分析文档第四阶段的标注评估集、PR/召回统计尚未建立，调参（`detector_tuning.json`）目前缺乏量化依据。
