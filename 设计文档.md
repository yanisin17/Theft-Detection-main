# 偷窃行为检测系统整体落地方案

> 核心定位：**不是 “识别小偷”，而是识别【拿取商品 → 遮挡藏匿】这一段连续行为**。
> 核心思路：**姿态做初筛 + 时序状态机做行为判定 + 人与商品空间关联**，这也是你现在代码正在迭代的方向。

## 整体架构（5 大模块）

1. **多路视频采集层**（你已经在做）
2. **目标 & 姿态推理层**（YOLO + MediaPipe Pose）
3. **时序行为状态机**（核心，区分「静态姿态」和「连续偷窃行为」）
4. **告警策略 & 二次复核层**（用来平衡召回 / 误报）
5. **业务层：存储、截图、录像、Web 看板、推送**

---

# 分步落地路线（分阶段，由易到难）

## 阶段 1：底座工程搭建（当前你所处阶段）

目标：稳定拿到多路 RTSP 视频、人体 + 商品检测、骨骼关键点，消除链路 bug

1. RTSP 采集
   - 每路摄像头独立采集线程，独立 YOLO Tracker（**你本次修复的重点，不能共用 tracker**）
   - RTSP TCP、5s 超时、指数退避重连、有界帧队列，只保留最新帧，丢弃过期帧
   - 帧携带采集时间戳（微秒），用于时序对齐
2. 推理流水线
   - YOLO：检测 `person` + `商品`；输出检测框、置信度、track_id
   - MediaPipe Pose：输出人体 33 关键点（骨骼），用于姿态判断
   - ✅ 坐标转换：裁剪人体区域后，商品框坐标要做偏移换算（本轮已经修复）
3. 输出：对每一个人（`cam_id+track_id`）持续输出：骨骼关键点、商品框、手部位置、ROI 区域

> 
> 阶段 1 目标：保证数据链路不出错，**拿到稳定、对齐的时序数据**。很多项目卡在这一步，算法本身没问题，但帧乱序、坐标错、track_id 跳变，后面行为识别全部失效。
> 启发：行为识别的前提是**稳定的时序跟踪**，没有稳定 track，后面的行为逻辑都是空中楼阁。

## 阶段 2：姿态特征提取（初筛模块）

目标：从骨骼提取**可疑姿态特征**，作为告警候选（姿态检测）
对每一帧的人体骨骼提取特征：

- 手在腋下 / 背后 / 身体遮挡区
- 弯腰下蹲、躯干遮挡手部
- 手腕被身体遮挡（关键点置信度低）
- 手长时间停留在身体贴身区域

输出：**姿态可疑分数（0~1）**

> 注意：**姿态只是候选，不能直接判定偷窃**。挠痒、整理衣服也会触发高分，这就是纯姿态方案误报高的根源。
> 工程做法：姿态分数高 → 标记这个 track_id 进入**行为观察状态**，进入状态机持续观测。

## 阶段 3：时序状态机（核心，升级到【行为盗窃检测】）

 
> 偷盗是**动作序列**：伸手取货 → 商品离开货架 → 移动到手 / 衣物遮挡区域 → 保持遮挡一段时间
> 状态机可以设计 4 个状态：

1. `IDLE` 空闲：正常行走，无异常姿态
2. `REACH` 伸手：手伸向货架，检测到商品靠近手部
3. `HIDE` 藏匿：商品被人体遮挡，同时手部处于身体贴身区域，持续 N 帧（比如 0.8~1.5s）
4. `ALERT` 告警：完整动作链达成，触发告警

**状态跳转规则**

- 从 IDLE → REACH：检测到手伸向货架，商品靠近手部
- REACH → HIDE：商品被人体遮挡，同时姿态特征为藏匿姿态，并且**持续一段时间**（过滤瞬间抬手）
- HIDE → ALERT：维持藏匿状态达到阈值，生成告警事件
- 任意状态如果长时间丢失目标 / 姿态恢复正常 → 回退到 IDLE，重置状态

> 
> 关键点：**状态机绑定 cam_id + track_id**，每个人独立一套状态，不会跨人、跨摄像头串状态。
> 这就是你本次代码增加独立 tracker 的意义：只有稳定的 track_id，状态机才能跟踪一段连续动作。

## 阶段 4：告警复核策略（用来平衡召回 & 误报，对应你代码里`REQUIRE_CORROBORATION`）

两级告警策略（参考前面调研的开源项目方案）

1. **一级（初筛）**：姿态 + 状态机触发候选告警（当前你开启的模式，优先保证召回，不漏偷）
2. **二级（佐证复核）**（后续迭代再打开）：
   - 校验：商品是否从货架 ROI 移出
   - 校验：商品是否持续被人体遮挡一定时长
   - 可选增强：VLM 视觉大模型做图片复核，看画面描述 “是否将商品藏入衣物”

> 
> 权衡：
> - 关闭二次佐证：召回高、漏报少，误报变多（当前方案）
> - 开启二次佐证：误报下降，但容易漏掉一些快速藏匿的真实偷盗行为

## 阶段 5：业务能力层

- 告警证据：微秒级截图、短视频片段、结构化告警事件（cam、track_id、时间、行为类型、分数）
- Web 前端：实时画面、ROI 配置、历史告警、事件回放
- 存储：事件数据库，不要无脑存全部视频，只存告警前后片段节省磁盘
- 可选：告警推送（企业微信 / 邮件）

## 阶段 6：效果评估与迭代（非常关键）

1. 收集真实门店视频，标注数据集：区分【正常拿取商品】、【藏匿偷盗】、【挠痒 / 整理衣物等误报样本】
2. 量化指标：
   - Recall 召回率：真实偷盗事件能不能检出（不漏）
   - Precision 精确率：告警里面真偷盗占比（少误报）
   - 平均告警延迟
3. 调参：状态机持续时间阈值、姿态分数阈值、商品 IOU 关联阈值

> 
> 重点：这个场景**不能只在测试视频上调参**，必须上线小流量采集真实样本持续迭代。

---

# 风险与难点（周报写思考，体现深度）

1. **通用 COCO 商品检测能力弱**：很难区分商品、包装袋、杂物。如果预算允许，需要采集门店图片微调 YOLO，做商品微调。
2. **遮挡下目标丢失**：商品被身体挡住后 YOLO 检测不到，此时只能依靠**时序状态推断**（商品消失前已经被手拿到，进入遮挡）。
3. **光线变化、顾客动作多样**：单纯固定规则容易误报，长期看可以在状态机输出的候选样本上训练轻量时序模型（比如 LSTM/Transformer）替代硬编码规则。
4. **算力压力**：多路摄像头，每路独立 tracker + 每帧 MediaPipe 推理，多路并发会吃 CPU/GPU，需要做推理调度降负载（例如静止画面降低推理频率）。