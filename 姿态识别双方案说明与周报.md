# 姿态识别双方案说明与周报

## 文档信息

- 项目：TheftGuard AI 偷盗行为检测系统
- 分析日期：2026-09-18
- 分析对象：实时行为检测器与聚合行为检测器
- 结论摘要：实时视频中两套方案同时运行；`RealtimeBehaviorDetector` 是主报警链路，`VideoBehaviorDetector` 是低频辅助和兜底链路；图片、视频上传接口只使用 `RealtimeBehaviorDetector`。

## 一、为什么会有两套方案

当前项目中的姿态识别不是单一算法，而是两个不同阶段形成的检测体系。

### 1. 聚合行为检测器

对应代码：

- `detection_engine/models/behavior/video_behavior.py`
- 类名：`VideoBehaviorDetector`

这是原先“项目 A”的行为识别引擎，覆盖 21 类行为规则，并保留 MediaPipe 关键点、规则引擎和可选的 XGBoost 分类分支。它更适合分析较长时间的行为，例如弯腰、遮挡商品、手在背后、头部转动、疑似抓取和藏匿组合。

但它最初并不适合直接承担实时主报警：

- 行为规则较多，调用频率较低。
- 原实现存在 33 点关键点使用 COCO 17 点索引的问题。
- 规则引擎、特征数据和 XGBoost 存在多套接口，结果容易不一致。
- 在实时循环中通常每 6 帧才执行一次，快速动作可能被漏掉。

### 2. 实时状态机检测器

对应代码：

- `detection_engine/realtime_behavior.py`
- 类名：`RealtimeBehaviorDetector`

这是后增加的实时专用方案，目标是稳定识别一个最核心的动作序列：

```text
伸手取物 → 手腕回缩 → 进入躯干、裤袋或髋部藏匿区
```

它只关注肩、肘、腕、髋等关键点，使用几何阈值和每人的状态机，能够每帧运行，适合实时视频报警。

因此，两套方案不是完全重复的同一种算法，而是“实时快速判断”和“多行为聚合分析”两个不同层次的实现。

## 二、公共输入链路

两套方案共享同一套上游输入：

```text
RTSP 摄像头
    ↓
ThreadedCamera 获取最新帧
    ↓
每路摄像头独立 YOLO Pose tracker
    ↓
人体框、track_id、17 点 YOLO 关键点
    ↓
按人体框向外扩展 60 像素并裁剪
    ├─ RealtimeBehaviorDetector：每帧分析
    └─ VideoBehaviorDetector：每 6 帧或弯腰时分析
```

YOLO Pose 负责“人员在哪里”和“这个人是谁的轨迹”，MediaPipe Pose 负责在单个人体裁剪图中提取更细的 33 点姿态。

## 三、方案一：RealtimeBehaviorDetector

### 3.1 定位

实时视频中的主报警检测器。

主要目标：

- 实时分析快速伸手动作。
- 判断手腕是否回缩到藏匿区域。
- 为“快速藏匿物品”生成报警分。

### 3.2 技术实现

- 输入：单人人体裁剪图。
- 姿态模型：MediaPipe Pose 33 点。
- 姿态配置：
  - `static_image_mode=True`
  - `model_complexity=1`
  - `min_detection_confidence=0.4`
  - `min_tracking_confidence=0.4`
- 每个 `cam_id:track_id` 拥有独立状态机。
- 每帧执行。

### 3.3 使用的关键点

| 部位 | MediaPipe 索引 |
| --- | --- |
| 左肩 | `11` |
| 右肩 | `12` |
| 左肘 | `13` |
| 右肘 | `14` |
| 左腕 | `15` |
| 右腕 | `16` |
| 左髋 | `23` |
| 右髋 | `24` |

关键点可见度低于 `0.35` 时，该侧手部信号不参与判断。

### 3.4 几何判断

系统首先计算：

- 肩膀中点。
- 髋部中点。
- 肩宽。
- 躯干纵向距离。
- 肩、肘、腕形成的肘关节角度。

满足以下任一条件时，认为手腕正在“探出”：

1. 手腕明显低于髋部。
2. 手腕水平距离大于肩宽的 `1.05` 倍，并且肘关节角度大于 `120°`。

满足以下区域条件时，认为手腕进入“藏匿区”：

- 手腕位于躯干中线附近，并处于肩部到腹部之间。
- 手腕位于髋部、裤袋或身体侧袋附近。

### 3.5 状态机

每个人的状态按以下流程运行：

```text
idle
  ↓ 连续检测到探出信号
reaching
  ↓ 手腕进入藏匿区且回缩距离达到阈值
Rapid Item Concealment
  ↓
状态复位
```

关键参数：

| 参数 | 当前值 | 含义 |
| --- | ---: | --- |
| `OUT_HITS_TO_REACH` | `2` | 连续探出次数 |
| `RETREAT_MIN` | `0.14` | 最小回缩距离 |
| `REACH_TIMEOUT` | `3.0` 秒 | 探出后未回缩的超时时间 |
| `REACH_DOWN_BELOW_HIP` | `0.03` | 低于髋部的归一化距离 |
| `REACH_OUT_SIDE_RATIO` | `1.05` | 水平探出相对肩宽的倍数 |
| `REACH_ARM_ANGLE_MIN` | `120°` | 向外探出的最小手臂角度 |

### 3.6 报警分数

回缩动作完成后，基础分数为：

```text
base = 0.50 + 0.30 × 回缩超额比例
```

附加分：

- 手腕位于髋部或裤袋：`+0.05`
- 手臂伸出较直：最多 `+0.04`

最终分数：

```text
alert_score = clamp(base + 位置加分 + 探出加分, 0.45, 0.85)
```

默认报警阈值：

```text
THEFT_ALERT_THRESHOLD = 0.6
```

当前 `REQUIRE_CORROBORATION=False`，一次完整的“探出→回缩藏匿”即可报警。

### 3.7 输出事件

| 事件 | 说明 |
| --- | --- |
| `Taking Object` | 低置信度动作提示，表示检测到伸手 |
| `Rapid Item Concealment` | 主要可报警事件，表示疑似快速藏匿物品 |

### 3.8 当前调用场景

- 实时摄像头视频循环：每帧运行。
- `/api/analyze/image`：逐个人体分析。
- `/api/analyze/video`：每 6 帧执行一次姿态跟踪，并复用实时状态机逻辑。

## 四、方案二：VideoBehaviorDetector

### 4.1 定位

聚合行为规则引擎，用于补充更多行为类型，并在实时状态机未确认报警时提供兜底。

它不是单纯的“第二套实时姿态识别”，而是包含：

- 21 类行为规则。
- MediaPipe 33 点姿态。
- 连续帧行为统计。
- 抓取与藏匿序列识别。
- 可选的 XGBoost 模型推断。

### 4.2 技术实现

- 输入：人体裁剪图。
- 检测结果：已经平移到裁剪图坐标系的商品框、类别和置信度。
- 姿态模型：MediaPipe Pose 33 点。
- 姿态配置：
  - `static_image_mode=False`
  - `model_complexity=1`
  - `smooth_landmarks=True`
  - `min_detection_confidence=0.5`
  - `min_tracking_confidence=0.5`
- 可选 XGBoost：
  - `models/standard/theft_xgb_model.pkl`
  - `models/enhanced/enhanced_theft_xgb_model.pkl`
  - 如果模型成功加载，会创建附加的 `ML Detected Theft` 分支。

### 4.3 主要行为规则

当前规则包括但不限于：

- 异常手臂姿态。
- 双手或单手放在背后。
- 疑似蹲姿。
- 不自然伸手。
- 靠近商品的抓取动作。
- 手腕向身体中心或口袋移动。
- 身体遮挡商品。
- 头部异常转动。
- 频繁头部移动。
- 抓取与藏匿的连续序列。

### 4.4 三层聚合计分

#### 第一层：规则加权

```text
rule_prob = Σ(行为置信度 × 行为权重)
```

例如：

- `rapid_item_concealment` 权重 `0.8`
- `item_concealment` 权重 `0.8`
- `item_grabbing` 权重 `0.8`
- `body_shielding` 权重 `0.7`
- `unusual_reaching` 权重 `0.7`

最终限制在 `0.0～1.0`。

#### 第二层：连续性加分

同一种行为连续出现：

- 连续 3 帧后开始加分。
- 每增加 1 帧增加 `0.05`。
- 最多增加 `0.15`。

#### 第三层：序列加分

如果同时出现以下组合，认为形成抓取和藏匿序列：

- `item_grabbing`、`rapid_item_concealment` 或 `item_concealment`
- 同时伴随 `single_arm_hiding`、`concealment_gesture` 或 `body_shielding`

序列成立时增加：

```text
sequence_bonus = 0.3
```

最终概率：

```text
theft_probability = min(
    1.0,
    rule_prob × 0.7 + continuity_bonus + sequence_bonus
)
```

当序列成立时，聚合引擎会把行为语义归一为：

```text
rapid_item_concealment
```

然后交给统一报警策略判断。

### 4.5 当前调用条件

实时循环中满足以下条件时执行：

```python
frame_count % BEHAVIOR_CHECK_INTERVAL == 0 or is_bending
```

当前 `BEHAVIOR_CHECK_INTERVAL=6`，即正常情况下平均每 6 帧执行一次。

### 4.6 优点与问题

优点：

- 行为类型覆盖更丰富。
- 有连续性和序列加分。
- 可以融合商品框和身体遮挡关系。
- 可接入 XGBoost 模型。

问题：

- 调用频率低于实时状态机，快速动作容易漏掉。
- 规则、权重和时间窗口较多，调参成本高。
- 与实时状态机同时运行会增加模型推理和状态维护成本。
- XGBoost 是否成功加载、模型是否适配当前特征，需要单独验证。
- 项目中的注释称其“用于离线文件分析”，但当前上传接口实际使用的是实时状态机，注释与实际调用不一致。

## 五、两套方案如何协同

### 5.1 实时视频

```text
每帧：
    RealtimeBehaviorDetector 分析“探出→回缩”

每 6 帧或弯腰：
    VideoBehaviorDetector 分析更多聚合行为

实时检测器已经确认报警：
    当前帧跳过 VideoBehaviorDetector

两者共用 PersonState：
    alert_fired 防止同一人重复报警
```

代码位置：

- 两套检测器初始化：`backend.py:1074`
- 实时检测器每帧调用：`backend.py:1289`
- 聚合检测器低频调用：`backend.py:1365`
- 聚合检测器跳过条件：`detection_engine/alert_policy.py:37`

### 5.2 上传图片和视频

当前两个接口使用：

```text
RealtimeBehaviorDetector
```

对应代码：

- 图片分析：`backend.py:1881`
- 视频分析：`backend.py:1953`

因此，上传文件和实时视频的报警主逻辑基本一致，`VideoBehaviorDetector` 没有接入这两个上传接口。

### 5.3 是否会重复报警

正常情况下不会重复报警：

1. 实时检测器在当前帧确认报警后，聚合检测器该帧会被跳过。
2. 两者共用 `PersonState`。
3. 同一个 `track_id` 的 `alert_fired=True` 后，另一套不会再次创建报警。

不过，两套检测器仍可能产生不同的分数和标签，因此日志、画面上可能同时出现两套概率信息。

## 六、当前实际运行结论

| 场景 | 主检测器 | 辅助检测器 | 说明 |
| --- | --- | --- | --- |
| 摄像头实时视频 | `RealtimeBehaviorDetector` | `VideoBehaviorDetector` | 两套都运行，前者每帧，后者每 6 帧或弯腰时 |
| 上传图片 | `RealtimeBehaviorDetector` | 无 | 逐个人体裁剪分析 |
| 上传视频 | `RealtimeBehaviorDetector` | 无 | 低频姿态跟踪后进入同一状态机 |
| 旧 `get_behavior_detectors()` | 未接入业务路由 | 无 | 遗留接口 |

一句话概括：

> 当前实时视频是“双引擎运行、单报警出口”；实时状态机负责快速报警，聚合规则引擎负责补充行为和兜底。上传图片、上传视频只使用实时状态机。

## 七、建议的后续处理

1. 明确 `RealtimeBehaviorDetector` 为唯一实时报警决策源。
2. 将 `VideoBehaviorDetector` 降级为行为特征提供器，只提供弯腰、遮挡、抓取等辅助特征，不直接报警。
3. 如果聚合引擎没有明显提升真实召回率，则从实时主循环移除，降低 CPU、GPU 和状态复杂度。
4. 统一行为名称、置信度名称和报警分，避免同一动作出现多套概率。
5. 修正“用于离线文件分析”的过时注释，并清理未接入路由的遗留函数。
6. 在真实视频上做两套方案的开关对比，分别统计：
   - 精确率
   - 召回率
   - F1
   - 每小时误报次数
   - 平均报警延迟
   - 单路和多路摄像头资源占用

## 八、周报可摘录内容

### 8.1 本周完成

1. 梳理了实时姿态识别的两条检测链路及共同输入流程。
2. 确认实时视频中 `RealtimeBehaviorDetector` 和 `VideoBehaviorDetector` 同时运行。
3. 确认实时状态机每帧执行，聚合规则引擎每 6 帧或弯腰时执行。
4. 确认上传图片和上传视频接口只使用 `RealtimeBehaviorDetector`。
5. 核对了两套状态机、行为规则、报警分数和去重机制。
6. 发现并记录双引擎重复计算、注释与调用不一致、遗留接口未接入等问题。

### 8.2 关键结论

- 当前实时报警主链路是 `RealtimeBehaviorDetector`。
- `VideoBehaviorDetector` 是低频辅助和兜底，不是唯一检测器。
- 两套检测器共享 `PersonState`，正常不会对同一人重复报警。
- 当前系统仍属于姿态动作预警，不能等同于精准偷窃判定。

### 8.3 风险与待验证项

- 双引擎运行会增加实时推理开销，需要完成多路摄像头压测。
- 聚合引擎的 XGBoost 模型是否有效、是否适配当前特征仍需验证。
- 两套分数和标签尚未完全统一，可能增加排障成本。
- 缺少真实标注数据，无法量化最终精确率和召回率。

### 8.4 下周计划

1. 明确第一版生产方案，以实时状态机作为主报警源。
2. 对聚合引擎进行开关消融测试，对比准确率、延迟和资源占用。
3. 统一报警分、行为类型和日志字段。
4. 清理未接入业务路由的遗留检测器加载逻辑。
5. 使用真实摄像头数据验证伸手、回缩、遮挡、弯腰和正常购物场景。

### 8.5 周报精简版

> 本周完成偷盗行为检测中两套姿态识别方案的代码梳理和运行链路确认。当前实时视频同时运行每帧执行的实时状态机和低频聚合行为引擎，其中实时状态机为主报警链路，聚合引擎负责多行为分析和兜底；上传图片、视频仅使用实时状态机。两套方案共享人员状态并可防止同一轨迹重复报警。已完成调用条件、关键点阈值、计分方式、协同关系和遗留问题整理。下一步将以实时状态机作为主决策源，对聚合引擎开展开关消融和真实场景效果、性能验证，再决定保留、降级或移除。

## 九、最终建议表述

向项目组汇报时建议使用以下表述：

> 当前系统已形成两套互补的行为检测能力。实时状态机负责快速捕捉“伸手→回缩藏匿”并触发实时报警；聚合规则引擎负责提供更多姿态、遮挡和序列行为分析，并在实时状态机未确认时进行兜底。现阶段以实时状态机为主报警源，聚合引擎作为辅助。后续需要通过真实数据评测决定聚合引擎是否保留在主报警链路，避免仅凭代码规则数量判断效果。
