# TheftGuard AI 项目整体分析

## 一、项目整体框架

项目当前是一条“摄像头采集 → 后端推理 → 行为规则判断 → WebSocket 推送 → 前端展示”的链路：

```text
RTSP/摄像头
  ↓
ThreadedCamera
  ↓
backend.video_loop()
  ├─ YOLO Pose：人体、关键点、跟踪
  ├─ YOLO Object：商品/物体检测
  ├─ MediaPipe：手部动作分析
  ├─ 规则状态机：伸手 → 回缩 → 疑似藏匿
  ├─ ROI、徘徊、黑名单判断
  └─ JPEG + Base64
       ↓
WebSocket /ws
       ↓
Next.js CameraGrid
```

主要入口是 `backend.py`，前端位于 `dashboard/src`，实时行为检测位于 `detection_engine/realtime_behavior.py`，离线及复杂行为逻辑主要位于 `detection_engine/models/behavior`。

## 二、为什么捕捉画面和识别结果不准

### 1. RTSP 可能使用了低质量子码流

`cameras.json` 中使用了海康 `Channels/102`，通常代表子码流。子码流常见问题包括分辨率低、压缩严重、帧率不稳定、目标尺寸小以及运动模糊。

后端虽然尝试设置 1280×720，但 `VideoCapture.set()` 对 RTSP 流并不能保证实际输出就是这个分辨率。项目需要记录每路摄像头的实际宽高、FPS、读取延迟和丢帧数量。

### 2. 多路摄像头共用串行推理循环

`backend.py` 的 `video_loop()` 顺序处理所有摄像头。每路视频都可能执行 YOLO Pose、跟踪、物体检测、MediaPipe、行为规则和 JPEG 编码。摄像头数量增加后，推理速度很容易低于输入 FPS。

结果是画面延迟、短动作丢失，以及模型看到的画面和用户实际看到的画面不同。`ThreadedCamera` 只覆盖旧帧，没有时间戳、队列长度、丢帧统计或断线重连机制。

### 3. 物体检测模型不是专门的偷盗模型

代码优先加载 `shoplifting.pt`，找不到时回退到 `models/yolov11n.pt`。仓库中没有明确的 `shoplifting.pt`，因此大概率使用的是通用 COCO 模型。

通用模型只能识别有限的物体类别，不能可靠判断商品是否被拿走、是否进入包或衣服、是否被身体遮挡，也不能直接判断动作是否构成偷盗。当前系统实际上是用“通用物体框 + 人体关键点 + 手写规则”推断偷盗，误报和漏报都比较容易发生。

### 4. 物体检测每 5 帧执行一次

代码通过 `frame_count % 5 == 0` 降低物体检测频率，其他帧复用上一轮的物体结果。这会导致快速移动时出现旧物体框、新物体漏检以及人和商品位置关联错误。

更合理的方式是使用独立的目标跟踪器，在检测帧之间跟踪商品和人员，而不是简单复用上一帧结果。

### 5. 行为分析存在裁剪坐标系问题

代码先根据人体框裁剪 `sub_frame`，随后把整幅图坐标的 `results_pose[0]` 传给裁剪图行为检测：

```python
sub_frame = frame[sy1:sy2, sx1:sx2].copy()
all_behaviors = behavior_detector.detect_behaviors_in_image(
    sub_frame,
    results_pose[0]
)
```

裁剪图坐标和整帧坐标没有做偏移转换，可能导致手腕、人体框和商品位置不对应，进而影响伸手、拿取和藏匿判断。这是当前识别不准的关键代码问题之一。

### 6. MediaPipe 规则过度依赖固定比例

实时行为识别大量使用固定阈值，例如手腕低于髋部、手腕距离身体中心、手臂角度和回缩距离。

这些阈值会受到摄像机俯视角度、人体远近、身高差异、左右镜像、关键点抖动和货架遮挡影响。因此弯腰、整理衣物、拿手机或正常查看商品，都可能被判定为伸手或藏匿。

另外，页面显示的置信度部分是规则计算和加权得到的，并不完全等价于经过校准的模型概率，不能直接理解为真实准确率。

### 7. ROI 画布可能产生坐标偏移

前端 ROI 画布固定为 1280×720，并将实时图片强制绘制到这个尺寸。如果实际视频分辨率或宽高比不同，用户点击的坐标就可能和后端原始帧坐标不一致。

可能出现的结果是 ROI 在前端看起来位于货架上，但后端实际判断区域发生偏移。ROI 必须基于实际视频尺寸，并统一处理缩放比例和边距偏移。

### 8. 可视化结果可能被后续绘制覆盖

后端先绘制热力图、行为文字和告警框，随后又调用 `results_pose[0].plot()` 并直接把返回图像赋给 `frame`。这会覆盖之前的自定义绘制。

因此用户看到的画面可能缺少行为文字、热力图或告警框，造成“后端判断和前端画面不一致”的问题。正确顺序应该是先生成 YOLO 结果图，再在该图上统一叠加所有自定义标注。

## 三、项目存在的其他问题

### 高优先级问题

- `cameras.json` 中存在明文摄像头账号密码，应立即修改密码并从仓库中移除。
- 后端监听 `0.0.0.0`，接口缺少身份认证，存在未授权访问风险。
- 截图文件名只精确到秒，同一秒内可能覆盖证据文件。
- 摄像头缺少断线自动重连、RTSP 超时和健康检查。
- 没有真实标注集，也没有 precision、recall、F1、误报率和漏报率统计。
- 现有测试主要验证规则函数，不能证明视频识别准确率。

### 中优先级问题

- `backend.py`、`video_behavior.py` 和 `realtime_behavior.py` 中存在多套行为识别逻辑，阈值和结果容易不一致。
- `requirements.txt`、实际运行环境和模型版本可能不一致，CPU/GPU 状态也需要确认。
- 项目中存在多个旧入口和重复实现，增加了维护和排错成本。
- 前端告警消息解析格式与后端实际消息格式不完全一致，部分告警只能显示原始英文文本。

## 四、改进建议

### 第一阶段：修复采集链路

1. 每路摄像头记录实际分辨率、FPS、读取延迟和丢帧数量。
2. RTSP 使用合适的主码流或高质量子码流。
3. 将视频缓冲区设置为较小值，避免推理旧帧。
4. 增加断线自动重连、超时和摄像头健康状态。
5. 每路摄像头使用独立采集线程，推理线程只取最新帧。
6. 同时保存原始帧和标注帧，区分采集问题与模型问题。

### 第二阶段：修复坐标和显示

1. 对人体裁剪后的关键点和检测框减去裁剪偏移量。
2. ROI 使用实际视频宽高，统一处理缩放和偏移。
3. 修复 `results_pose[0].plot()` 覆盖自定义标注的问题。
4. 前端保持原始宽高比，不强制拉伸视频。
5. 为每帧附加时间戳，避免把旧帧误认为当前帧。

### 第三阶段：重新设计检测模型

1. 使用真实商店场景数据训练商品、货架、手、包和身体遮挡区域。
2. 不再使用通用 COCO 模型直接推断偷盗行为。
3. 使用 YOLO 检测配合 ByteTrack 或 BoT-SORT 跟踪。
4. 建立“人—手—商品—货架”的空间关联关系。
5. 使用连续 8～15 帧的时序判断，而不是单帧阈值。
6. 针对不同摄像头分别标定阈值。

### 第四阶段：建立评估体系

应准备正常购物、正常拿取和放回商品、真实藏匿、弯腰、打电话、整理衣物、不同距离、不同角度、不同光照和遮挡条件的视频。

至少统计以下指标：

```text
Precision：报警中有多少是真的
Recall：真实偷盗中有多少被发现
False Alarm Rate：每小时误报次数
Detection Latency：动作发生到报警的时间
```

## 五、结论

当前识别不准不是单纯因为模型太小，而是多个环节共同造成的：

1. RTSP 子码流质量和实际分辨率不稳定；
2. 多摄像头串行推理造成延迟和短动作丢失；
3. 通用目标检测模型不适合直接判断偷盗；
4. 物体检测结果间隔更新，容易使用过期框；
5. 裁剪图与整帧关键点存在坐标系不一致；
6. 行为规则固定阈值过多，缺少场景标定；
7. ROI 画布和实际视频尺寸可能不一致；
8. 自定义可视化会被 YOLO 绘图覆盖；
9. 项目缺少真实数据集和量化评估。

建议优先修复采集稳定性、坐标变换、ROI 映射、标注覆盖和运行监控，再重新训练适合商店场景的检测模型。否则单纯更换更大的 YOLO 模型，通常不能根本解决误报和漏报问题。
