# ============================================================
# TheftGuard AI - 智能防盗监控系统
# Python 3.9 - 3.11 推荐（实测 3.11 通过）
# 安装：pip install -r requirements.txt
# ============================================================

# ---------- Web 框架 ----------
fastapi==0.141.1
uvicorn==0.52.4
websockets==17.1
pydantic==2.13.5
python-multipart==0.0.32          # FastAPI 文件上传
python-dotenv==1.2.3              # .env 配置

# ---------- 计算机视觉 & 深度学习 ----------
opencv-python==4.11.0.86
opencv-contrib-python==4.11.0.86
ultralytics==8.3.0                # YOLOv8-pose + YOLOv11n
# torch 建议按官方指南安装 CUDA 版本以启用 GPU 加速
# https://pytorch.org/get-started/locally/
torch==2.4.1+cu124
torchvision==0.19.1+cu124
--extra-index-url https://download.pytorch.org/whl/cu124
numpy==1.26.4                     # 必须 <2.0（ultralytics 8.3.0 不兼容 numpy 2.x）
Pillow==12.3.0

# ---------- 行为识别引擎（MediaPipe + XGBoost + 规则引擎） ----------
mediapipe==0.10.14                # 33关键点姿态估计（必须 0.10.x，不要用 1.0+）
xgboost==3.2.0                    # 行为分类器
scikit-learn==1.9.0              # XGBoost 特征预处理
scipy==1.17.1
pandas==3.0.5

# ---------- 可视化 ----------
matplotlib==3.11.1
seaborn==0.13.2

# ---------- 系统监控 ----------
psutil==7.2.2                    # CPU/RAM 实时占用

# ---------- 通知 ----------
requests==2.34.2                 # Telegram API

# ---------- 人脸识别（可选，Windows 难装） ----------
# Windows 安装需 Visual Studio Build Tools + CMake
# 不装则人脸面板自动禁用，不影响行为识别
# face_recognition==1.3.0

# ============================================================
# 说明：
# 1. numpy 必须保持 1.26.4，升到 2.x 会导致 ultralytics 8.3.0 报错
# 2. mediapipe 必须保持 0.10.x，1.0+ 会强制升级 numpy 到 2.x
# 3. torch 的 cu124 版本需要 NVIDIA GPU + CUDA 12.4 驱动
#    没有 GPU 可改为：torch==2.4.1（CPU 版）
# 4. face_recognition 注释掉了，需要的话取消注释
# 5. sqlite3 / smtplib / email / threading / uuid 等为 Python 标准库
# ============================================================
